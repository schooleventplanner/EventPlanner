

===== FILE: E:\School Event Planner\frontend\src\App.jsx =====

import { BrowserRouter, Routes, Route } from "react-router-dom";

import Home from "./pages/Home";
import Login from "./pages/Login";
import Register from "./pages/Register";
import Events from "./pages/Events";
import EventDetail from "./pages/EventDetail";
import Announcements from "./pages/Announcements";
import Dashboard from "./pages/Dashboard";
import StaffDashboard from "./pages/StaffDashboard";

import "./index.css";

function App() {
  return (
    <BrowserRouter>
      <Routes>
        {/* PUBLIC */}
        <Route path="/" element={<Home />} />
        <Route path="/events" element={<Events />} />
        <Route
          path="/events/:id"
          element={<EventDetail />}
        />
        <Route
          path="/announcements"
          element={<Announcements />}
        />

        {/* AUTH */}
        <Route path="/login" element={<Login />} />
        <Route path="/register" element={<Register />} />

        {/* PARTICIPANT */}
        <Route
          path="/dashboard"
          element={<Dashboard />}
        />

        {/* STAFF */}
        <Route
          path="/admin"
          element={<StaffDashboard />}
        />
      </Routes>
    </BrowserRouter>
  );
}

export default App;


===== FILE: E:\School Event Planner\frontend\src\index.css =====

@import url("https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&family=Manrope:wght@500;600;700;800&display=swap");

:root {
  --red-900: #8f0d17;
  --red-800: #a70f1b;
  --red-700: #c31320;
  --red-600: #df1f2d;
  --red-100: #fff0f1;
  --red-50: #fff7f7;
  --ink: #15171b;
  --muted: #6e727a;
  --line: #e9e9ec;
  --surface: #ffffff;
  --soft: #f7f7f8;
  --green: #16834b;
  --shadow: 0 20px 60px rgba(21, 23, 27, 0.09);
  --radius: 22px;
}

* {
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

body {
  margin: 0;
  background: var(--surface);
  color: var(--ink);
  font-family: "DM Sans", sans-serif;
}

button,
input,
textarea,
select {
  font: inherit;
}

button,
a {
  -webkit-tap-highlight-color: transparent;
}

a {
  color: inherit;
  text-decoration: none;
}

button {
  cursor: pointer;
}

button:disabled {
  cursor: not-allowed;
  opacity: 0.58;
}

.page,
.home-page,
.dashboard-page,
.staff-page,
.auth-page {
  min-height: 100vh;
}

.page-container {
  width: min(1180px, calc(100% - 40px));
  margin: 0 auto;
}

.app-header {
  min-height: 76px;
  padding: 0 max(20px, calc((100vw - 1180px) / 2));
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 24px;
  background: rgba(255, 255, 255, 0.94);
  border-bottom: 1px solid var(--line);
  position: relative;
  z-index: 20;
  backdrop-filter: blur(18px);
}

.brand,
.auth-brand-logo {
  display: inline-flex;
  align-items: center;
  gap: 11px;
  font-family: "Manrope", sans-serif;
  font-weight: 700;
}

.brand-mark {
  width: 40px;
  height: 40px;
  display: grid;
  place-items: center;
  flex: 0 0 auto;
  border-radius: 12px;
  background: var(--red-700);
  color: white;
  font-size: 13px;
  letter-spacing: 0.04em;
  box-shadow: 0 9px 22px rgba(195, 19, 32, 0.24);
}

.brand-name {
  color: var(--ink);
  line-height: 1.05;
}

.brand-name strong {
  color: var(--red-700);
  display: block;
}

.header-actions {
  display: flex;
  align-items: center;
  gap: 9px;
}

.header-link,
.header-logout {
  padding: 10px 14px;
  color: #5f636b;
  border: 0;
  background: transparent;
  border-radius: 11px;
  transition: 0.2s ease;
}

.header-link:hover,
.header-link.active,
.header-logout:hover {
  color: var(--red-700);
  background: var(--red-50);
}

.header-cta,
.header-logout {
  border: 1px solid var(--line);
  background: white;
  border-radius: 11px;
  font-weight: 700;
}

.header-cta {
  padding: 11px 16px;
  color: white;
  background: var(--red-700);
  border-color: var(--red-700);
  box-shadow: 0 7px 20px rgba(195, 19, 32, 0.18);
}

.header-cta:hover {
  background: var(--red-800);
}

.header-logout {
  padding: 10px 15px;
}

.eyebrow {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  color: var(--red-700);
  font-size: 11px;
  font-weight: 800;
  letter-spacing: 0.16em;
}

.eyebrow.light {
  color: rgba(255, 255, 255, 0.72);
}

h1,
h2,
h3,
p {
  margin-top: 0;
}

h1,
h2,
h3 {
  font-family: "Manrope", sans-serif;
}

.btn {
  min-height: 48px;
  padding: 0 20px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 12px;
  border: 0;
  border-radius: 13px;
  font-weight: 800;
  transition: transform 0.2s ease, box-shadow 0.2s ease, background 0.2s ease;
}

.btn:hover:not(:disabled) {
  transform: translateY(-2px);
}

.btn-primary {
  color: white;
  background: var(--red-700);
  box-shadow: 0 12px 28px rgba(195, 19, 32, 0.2);
}

.btn-primary:hover:not(:disabled) {
  background: var(--red-800);
}

.btn-secondary {
  color: var(--ink);
  background: #f2f2f4;
}

.btn-white {
  color: var(--red-800);
  background: white;
}

.btn-outline-light {
  color: white;
  background: transparent;
  border: 1px solid rgba(255, 255, 255, 0.28);
}

.full {
  width: 100%;
}

.inline-link {
  color: var(--red-700);
  font-size: 14px;
  font-weight: 800;
}

.inline-link:hover {
  text-decoration: underline;
}

.section-heading {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: 24px;
  margin-bottom: 30px;
}

.section-heading h2 {
  margin: 8px 0 0;
  font-size: clamp(26px, 3vw, 36px);
  letter-spacing: -0.04em;
}

.modern-state {
  min-height: 260px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  gap: 8px;
  padding: 30px;
  text-align: center;
  border: 1px solid var(--line);
  border-radius: var(--radius);
  background: #fff;
  color: var(--muted);
}

.modern-state strong {
  color: var(--ink);
  font-family: "Manrope", sans-serif;
}

.loading-spinner {
  width: 34px;
  height: 34px;
  margin-bottom: 5px;
  border: 3px solid #f0d6d8;
  border-top-color: var(--red-700);
  border-radius: 50%;
  animation: spin 0.8s linear infinite;
}

@keyframes spin {
  to {
    transform: rotate(360deg);
  }
}

.state-card,
.empty-card {
  padding: 46px;
  border: 1px solid var(--line);
  border-radius: var(--radius);
  background: white;
  box-shadow: 0 12px 38px rgba(20, 22, 26, 0.04);
}

.empty-card {
  text-align: center;
}

.empty-card.large {
  min-height: 300px;
  display: grid;
  place-items: center;
  align-content: center;
  gap: 7px;
}

.empty-card h3,
.state-card h3 {
  margin: 8px 0 6px;
  font-size: 23px;
}

.empty-card p,
.state-card p {
  color: var(--muted);
  max-width: 500px;
  margin-bottom: 20px;
}

.empty-icon {
  width: 52px;
  height: 52px;
  display: grid;
  place-items: center;
  margin: 0 auto 12px;
  border-radius: 16px;
  color: var(--red-700);
  background: var(--red-100);
  font-size: 26px;
}

/* HOME */

.home-hero,
.events-hero {
  color: white;
  background:
    radial-gradient(circle at 78% 35%, rgba(255, 255, 255, 0.11), transparent 25%),
    radial-gradient(circle at 15% 90%, rgba(255, 255, 255, 0.08), transparent 30%),
    linear-gradient(135deg, #a70f1b 0%, #c31320 48%, #8f0d17 100%);
}

.home-hero {
  min-height: 650px;
  display: flex;
  align-items: center;
  overflow: hidden;
}

.home-hero-grid {
  min-height: 650px;
  display: grid;
  grid-template-columns: 1.05fr 0.95fr;
  align-items: center;
  gap: 50px;
}

.home-copy h1,
.events-hero h1 {
  margin: 14px 0 22px;
  font-size: clamp(46px, 6vw, 76px);
  line-height: 0.98;
  letter-spacing: -0.065em;
}

.home-copy h1 span,
.events-hero h1 span {
  color: #ffdfe2;
}

.home-copy p,
.events-hero p {
  max-width: 610px;
  margin-bottom: 30px;
  color: rgba(255, 255, 255, 0.76);
  font-size: 17px;
  line-height: 1.75;
}

.hero-buttons {
  display: flex;
  flex-wrap: wrap;
  gap: 11px;
}

.trust-note {
  display: flex;
  align-items: center;
  gap: 9px;
  margin-top: 25px;
  color: rgba(255, 255, 255, 0.66);
  font-size: 13px;
}

.pulse-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: #ffd6d9;
  box-shadow: 0 0 0 5px rgba(255, 214, 217, 0.1);
}

.hero-visual {
  min-height: 500px;
  position: relative;
  display: grid;
  place-items: center;
}

.hero-orb {
  position: absolute;
  border-radius: 50%;
  border: 1px solid rgba(255, 255, 255, 0.13);
}

.orb-one {
  width: 440px;
  height: 440px;
}

.orb-two {
  width: 310px;
  height: 310px;
}

.event-poster {
  width: min(340px, 72%);
  min-height: 410px;
  padding: 24px;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  position: relative;
  z-index: 2;
  border: 1px solid rgba(255, 255, 255, 0.2);
  border-radius: 25px;
  background: rgba(255, 255, 255, 0.12);
  box-shadow: 0 35px 80px rgba(60, 0, 5, 0.28);
  backdrop-filter: blur(15px);
  transform: rotate(3deg);
}

.poster-top,
.poster-bottom {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 15px;
  color: rgba(255, 255, 255, 0.68);
  font-size: 11px;
  font-weight: 800;
  letter-spacing: 0.13em;
}

.poster-status {
  padding: 6px 9px;
  border-radius: 20px;
  color: white;
  background: rgba(255, 255, 255, 0.13);
}

.poster-day {
  margin-top: 10px;
  font-family: "Manrope", sans-serif;
  font-size: 130px;
  line-height: 0.9;
  font-weight: 800;
  letter-spacing: -0.09em;
}

.poster-title {
  padding: 25px 0;
  border-top: 1px solid rgba(255, 255, 255, 0.18);
}

.poster-title span {
  display: block;
  margin-bottom: 7px;
  color: #ffd4d7;
  font-size: 11px;
  font-weight: 800;
  letter-spacing: 0.16em;
}

.poster-title strong {
  font-family: "Manrope", sans-serif;
  font-size: 29px;
  line-height: 1.04;
}

.floating-card {
  position: absolute;
  z-index: 3;
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 15px;
  border: 1px solid rgba(255, 255, 255, 0.2);
  border-radius: 16px;
  background: rgba(255, 255, 255, 0.13);
  box-shadow: 0 20px 50px rgba(60, 0, 5, 0.22);
  backdrop-filter: blur(15px);
}

.floating-events {
  top: 15%;
  right: 1%;
}

.floating-events strong {
  font-size: 29px;
}

.floating-events span,
.floating-new small {
  color: rgba(255, 255, 255, 0.62);
  font-size: 11px;
}

.floating-new {
  left: 1%;
  bottom: 14%;
}

.floating-plus {
  width: 38px;
  height: 38px;
  display: grid;
  place-items: center;
  border-radius: 12px;
  color: var(--red-700);
  background: white;
  font-size: 22px;
}

.floating-new strong {
  display: block;
  font-size: 13px;
}

.home-stats {
  border-bottom: 1px solid var(--line);
  background: white;
}

.stats-strip {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
}

.stats-strip > div {
  padding: 28px 25px;
  border-right: 1px solid var(--line);
}

.stats-strip > div:first-child {
  border-left: 1px solid var(--line);
}

.stats-strip strong {
  display: block;
  margin-bottom: 3px;
  font-family: "Manrope", sans-serif;
  font-size: 28px;
}

.stats-strip span {
  color: var(--muted);
  font-size: 13px;
}

.home-events {
  padding-top: 105px;
  padding-bottom: 110px;
}

.home-event-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 18px;
}

.home-event-card,
.event-card {
  position: relative;
  overflow: hidden;
  border: 1px solid var(--line);
  border-radius: var(--radius);
  background: white;
  transition: 0.25s ease;
}

.home-event-card {
  min-height: 345px;
  padding: 24px;
}

.home-event-card:hover,
.event-card:hover {
  border-color: #efc4c7;
  box-shadow: var(--shadow);
  transform: translateY(-5px);
}

.event-card-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.tag {
  display: inline-flex;
  align-items: center;
  width: fit-content;
  padding: 6px 9px;
  border-radius: 8px;
  color: var(--red-700);
  background: var(--red-100);
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 0.08em;
}

.event-card-arrow {
  color: var(--red-700);
  font-size: 20px;
}

.home-event-index {
  display: block;
  margin: 42px 0 10px;
  color: #b9bcc2;
  font-size: 11px;
  font-weight: 800;
  letter-spacing: 0.1em;
}

.home-event-card h3 {
  margin-bottom: 9px;
  font-size: 25px;
  letter-spacing: -0.035em;
}

.home-event-card p,
.event-card-body p {
  color: var(--muted);
  line-height: 1.65;
}

.home-event-footer {
  position: absolute;
  right: 24px;
  bottom: 24px;
  left: 24px;
  display: flex;
  justify-content: space-between;
  gap: 10px;
  padding-top: 16px;
  border-top: 1px solid var(--line);
  color: #62666e;
  font-size: 12px;
}

.home-cta {
  padding: 82px 0;
  color: white;
  background: var(--red-700);
}

.cta-inner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 40px;
}

.cta-inner h2 {
  margin: 10px 0 0;
  font-size: clamp(32px, 4vw, 50px);
  line-height: 1.05;
  letter-spacing: -0.055em;
}

.cta-button {
  min-width: 165px;
  min-height: 165px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  gap: 9px;
  border: 1px solid rgba(255, 255, 255, 0.3);
  border-radius: 50%;
  color: white;
  background: rgba(255, 255, 255, 0.09);
  transition: 0.25s ease;
}

.cta-button:hover {
  background: white;
  color: var(--red-700);
  transform: rotate(-4deg);
}

.cta-button span {
  max-width: 90px;
  text-align: center;
  font-size: 13px;
  font-weight: 800;
}

.app-footer {
  padding: 25px 0;
  background: #121316;
  color: #858990;
}

.footer-inner,
.footer-brand {
  display: flex;
  align-items: center;
}

.footer-inner {
  justify-content: space-between;
  gap: 25px;
  font-size: 12px;
}

.footer-brand {
  gap: 11px;
}

.footer-brand .brand-mark {
  width: 34px;
  height: 34px;
  font-size: 11px;
}

.footer-brand strong,
.footer-brand span {
  display: block;
}

.footer-brand strong {
  color: white;
  margin-bottom: 3px;
}

/* EVENTS */

.events-hero {
  padding: 90px 0;
}

.events-hero-grid {
  display: grid;
  grid-template-columns: 1fr auto;
  align-items: end;
  gap: 50px;
}

.events-hero h1 {
  margin-bottom: 0;
}

.hero-counter {
  display: flex;
  align-items: flex-end;
  gap: 14px;
  padding-bottom: 10px;
}

.hero-counter strong {
  font-family: "Manrope", sans-serif;
  font-size: 88px;
  line-height: 0.8;
  letter-spacing: -0.08em;
}

.hero-counter span {
  padding-bottom: 3px;
  color: rgba(255, 255, 255, 0.62);
  font-size: 13px;
  line-height: 1.4;
}

.events-browser {
  padding-top: 85px;
  padding-bottom: 100px;
}

.filter-group {
  display: flex;
  flex-wrap: wrap;
  gap: 7px;
  padding: 5px;
  border: 1px solid var(--line);
  border-radius: 13px;
  background: #fafafa;
}

.filter {
  padding: 10px 14px;
  border: 0;
  border-radius: 9px;
  color: var(--muted);
  background: transparent;
  font-size: 13px;
  font-weight: 700;
}

.filter.active {
  color: white;
  background: var(--red-700);
  box-shadow: 0 6px 14px rgba(195, 19, 32, 0.16);
}

.event-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 18px;
}

.event-card {
  min-height: 380px;
  padding: 25px;
  display: flex;
  flex-direction: column;
}

.event-number {
  color: #b7bac0;
  font-size: 11px;
  font-weight: 800;
}

.event-card-body {
  margin-top: 48px;
}

.event-card-body h3 {
  margin-bottom: 10px;
  font-size: 28px;
  letter-spacing: -0.045em;
}

.event-meta {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 15px;
  margin-top: auto;
  padding-top: 20px;
  border-top: 1px solid var(--line);
}

.event-meta span,
.staff-limit span,
.info-card span {
  display: block;
  margin-bottom: 6px;
  color: #9a9da3;
  font-size: 9px;
  font-weight: 800;
  letter-spacing: 0.1em;
}

.event-meta strong {
  display: block;
  font-size: 12px;
  line-height: 1.45;
}

.event-card-arrow {
  position: absolute;
  top: 24px;
  right: 24px;
}

/* DETAIL */

.detail-hero {
  padding: 75px 0 90px;
  color: white;
  background:
    radial-gradient(circle at 85% 20%, rgba(255,255,255,.12), transparent 25%),
    linear-gradient(135deg, #9d0d18, #d11b28);
}

.back-link {
  color: rgba(255,255,255,.72);
  font-size: 13px;
  font-weight: 700;
}

.back-link:hover {
  color: white;
}

.detail-hero-content {
  max-width: 820px;
  margin-top: 65px;
}

.tag-row {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 8px;
}

.tag-light {
  color: white;
  background: rgba(255,255,255,.13);
  border: 1px solid rgba(255,255,255,.18);
}

.hero-year {
  color: rgba(255,255,255,.65);
  font-size: 12px;
  font-weight: 700;
}

.detail-hero h1 {
  margin: 18px 0 15px;
  font-size: clamp(42px, 6vw, 70px);
  line-height: 1;
  letter-spacing: -0.065em;
}

.detail-hero p {
  max-width: 720px;
  color: rgba(255,255,255,.75);
  font-size: 16px;
  line-height: 1.75;
}

.detail-content {
  padding: 80px 0 110px;
  background: #fafafa;
}

.detail-grid {
  display: grid;
  grid-template-columns: minmax(0, 1fr) 390px;
  align-items: start;
  gap: 65px;
}

.detail-main h2 {
  margin: 8px 0 15px;
  font-size: 36px;
  letter-spacing: -0.045em;
}

.detail-description {
  max-width: 720px;
  color: var(--muted);
  line-height: 1.8;
}

.info-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 13px;
  margin-top: 40px;
}

.info-card {
  padding: 22px;
  border: 1px solid var(--line);
  border-radius: 17px;
  background: white;
}

.info-card strong {
  font-size: 15px;
}

.registration-card-panel {
  padding: 27px;
  position: sticky;
  top: 100px;
  border: 1px solid var(--line);
  border-radius: 23px;
  background: white;
  box-shadow: var(--shadow);
}

.availability {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-bottom: 35px;
  color: var(--green);
  font-size: 12px;
  font-weight: 800;
}

.dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: #9b9da2;
}

.dot.open {
  background: var(--green);
  box-shadow: 0 0 0 5px rgba(22,131,75,.09);
}

.registration-card-panel h2 {
  margin-bottom: 9px;
  font-size: 28px;
  letter-spacing: -0.04em;
}

.registration-card-panel p {
  color: var(--muted);
  line-height: 1.7;
}

.panel-note {
  display: block;
  margin-top: 13px;
  color: #979aa1;
  font-size: 11px;
  line-height: 1.5;
}

.form-alert,
.form-success {
  display: flex;
  align-items: flex-start;
  gap: 10px;
  margin: 15px 0;
  padding: 12px 14px;
  border-radius: 12px;
  font-size: 12px;
  line-height: 1.5;
}

.form-alert {
  color: #8e151d;
  background: #fff0f1;
  border: 1px solid #ffd4d7;
}

.form-success {
  color: #146a3d;
  background: #edf9f2;
  border: 1px solid #ccebd8;
}

.success-box {
  text-align: center;
}

.success-icon {
  width: 54px;
  height: 54px;
  display: grid;
  place-items: center;
  margin: 0 auto 18px;
  border-radius: 50%;
  color: white;
  background: var(--green);
  font-size: 24px;
}

.success-box .eyebrow {
  justify-content: center;
}

.not-found {
  width: min(620px, calc(100% - 40px));
  margin: 100px auto;
  text-align: center;
}

.not-found-code {
  display: block;
  color: var(--red-700);
  font-family: "Manrope", sans-serif;
  font-size: 90px;
  font-weight: 800;
  letter-spacing: -0.09em;
}

/* DASHBOARD */

.dashboard-page {
  background: #fafafa;
}

.dashboard-header {
  background: white;
}

.profile-mini {
  display: flex;
  align-items: center;
  gap: 9px;
  padding: 5px 10px 5px 5px;
  border: 1px solid var(--line);
  border-radius: 30px;
  font-size: 13px;
  font-weight: 700;
}

.avatar {
  width: 31px;
  height: 31px;
  display: grid;
  place-items: center;
  border-radius: 50%;
  color: white;
  background: var(--red-700);
  font-size: 12px;
}

.dashboard-shell {
  width: min(1180px, calc(100% - 40px));
  margin: 0 auto;
  padding: 65px 0 100px;
}

.dashboard-hero {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: 30px;
  margin-bottom: 42px;
}

.dashboard-hero h1 {
  margin: 10px 0;
  font-size: clamp(38px, 5vw, 58px);
  letter-spacing: -0.06em;
}

.dashboard-hero h1 span {
  color: var(--red-700);
}

.dashboard-hero p {
  margin: 0;
  color: var(--muted);
}

.stats-grid,
.staff-stats {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 13px;
}

.stat-card,
.staff-stat {
  padding: 22px;
  border: 1px solid var(--line);
  border-radius: 18px;
  background: white;
}

.stat-card.accent,
.staff-stat.accent {
  color: white;
  background: var(--red-700);
  border-color: var(--red-700);
}

.stat-card > span,
.staff-stat > span {
  display: block;
  margin-bottom: 16px;
  font-size: 10px;
  font-weight: 800;
  letter-spacing: .12em;
  color: #8b8e95;
}

.stat-card.accent > span,
.staff-stat.accent > span {
  color: rgba(255,255,255,.7);
}

.stat-card strong,
.staff-stat strong {
  display: block;
  margin-bottom: 5px;
  font-family: "Manrope", sans-serif;
  font-size: 34px;
  letter-spacing: -0.05em;
}

.stat-card small,
.staff-stat small {
  color: #989ba1;
  font-size: 11px;
}

.stat-card.accent small,
.staff-stat.accent small {
  color: rgba(255,255,255,.65);
}

.active-text {
  color: var(--green);
}

.content-section {
  margin-top: 85px;
}

.registration-list,
.staff-event-list {
  display: grid;
  gap: 12px;
}

.registration-card {
  min-height: 115px;
  display: grid;
  grid-template-columns: 90px minmax(0, 1fr) auto;
  align-items: center;
  gap: 22px;
  padding: 15px;
  border: 1px solid var(--line);
  border-radius: 18px;
  background: white;
  transition: .2s ease;
}

.registration-card:hover {
  border-color: #efc4c7;
  box-shadow: 0 15px 35px rgba(20,22,26,.05);
}

.date-tile {
  min-height: 83px;
  display: grid;
  place-items: center;
  align-content: center;
  border-radius: 13px;
  color: var(--red-700);
  background: var(--red-50);
}

.date-tile span {
  font-size: 9px;
  font-weight: 800;
  letter-spacing: .1em;
}

.date-tile strong {
  font-size: 13px;
}

.registration-main h3 {
  margin: 8px 0 5px;
  font-size: 20px;
}

.registration-main p {
  margin: 0;
  color: var(--muted);
  font-size: 12px;
}

.status {
  display: inline-flex;
  width: fit-content;
  padding: 6px 8px;
  border-radius: 8px;
  font-size: 10px;
  font-weight: 800;
}

.status.approved {
  color: #137242;
  background: #ebf8f0;
}

.status.pending {
  color: #8d6712;
  background: #fff6dc;
}

.status.closed {
  color: #70737a;
  background: #f0f0f2;
}

.card-link {
  padding: 10px 13px;
  color: var(--red-700);
  font-size: 12px;
  font-weight: 800;
}

/* AUTH */

.auth-page {
  min-height: 100vh;
  background: #f7f7f8;
}

.auth-shell {
  min-height: 100vh;
  display: grid;
  grid-template-columns: 0.9fr 1.1fr;
}

.auth-brand {
  padding: 42px;
  display: flex;
  flex-direction: column;
  color: white;
  background:
    radial-gradient(circle at 75% 30%, rgba(255,255,255,.12), transparent 25%),
    linear-gradient(145deg, #9d0d18, #c31320 55%, #870b15);
}

.auth-brand-logo {
  color: white;
}

.auth-brand-logo .brand-mark {
  color: var(--red-700);
  background: white;
  box-shadow: none;
}

.auth-brand-copy {
  max-width: 540px;
  margin: auto 0;
}

.auth-brand-copy h1 {
  margin: 15px 0 20px;
  font-size: clamp(42px, 5vw, 67px);
  line-height: .98;
  letter-spacing: -.07em;
}

.auth-brand-copy h1 span {
  color: #ffdce0;
}

.auth-brand-copy p {
  max-width: 470px;
  color: rgba(255,255,255,.72);
  line-height: 1.75;
}

.auth-brand-footer {
  display: flex;
  justify-content: space-between;
  color: rgba(255,255,255,.55);
  font-size: 10px;
  font-weight: 800;
  letter-spacing: .15em;
}

.auth-form-side {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 45px;
  background: white;
}

.auth-form-box {
  width: min(440px, 100%);
}

.auth-form-box h2 {
  margin: 10px 0 8px;
  font-size: 37px;
  letter-spacing: -.055em;
}

.auth-subtitle {
  margin-bottom: 30px;
  color: var(--muted);
}

.auth-form {
  display: grid;
  gap: 18px;
}

.field {
  display: grid;
  gap: 7px;
}

.field label {
  font-size: 12px;
  font-weight: 800;
}

.field input,
.field textarea,
.field select {
  width: 100%;
  border: 1px solid #dedfe3;
  outline: none;
  border-radius: 12px;
  padding: 13px 14px;
  color: var(--ink);
  background: white;
  transition: .2s ease;
}

.field input,
.field select {
  min-height: 47px;
}

.field textarea {
  resize: vertical;
  min-height: 110px;
}

.field input::placeholder,
.field textarea::placeholder {
  color: #afb2b8;
}

.field input:focus,
.field textarea:focus,
.field select:focus {
  border-color: var(--red-600);
  box-shadow: 0 0 0 4px rgba(223,31,45,.08);
}

.field-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 13px;
}

.auth-switch {
  display: flex;
  justify-content: center;
  gap: 5px;
  margin-top: 23px;
  color: var(--muted);
  font-size: 12px;
}

.auth-switch a,
.auth-back {
  color: var(--red-700);
  font-weight: 800;
}

.auth-back {
  display: block;
  width: fit-content;
  margin: 25px auto 0;
  font-size: 12px;
}

.auth-mobile-logo {
  display: none;
}

/* STAFF */

.staff-page {
  background: #f8f8f9;
}

.staff-header {
  background: white;
}

.staff-badge {
  padding: 7px 10px;
  border-radius: 8px;
  color: var(--red-700);
  background: var(--red-100);
  font-size: 10px;
  font-weight: 800;
  letter-spacing: .1em;
}

.staff-hero {
  padding-top: 60px;
  padding-bottom: 42px;
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: 30px;
}

.staff-hero h1 {
  margin: 10px 0;
  font-size: clamp(42px, 5vw, 60px);
  letter-spacing: -.065em;
}

.staff-hero p {
  margin: 0;
  color: var(--muted);
}

.staff-stats {
  width: min(1180px, calc(100% - 40px));
  margin: 0 auto;
}

.staff-content {
  padding-top: 70px;
  padding-bottom: 100px;
}

.result-count {
  color: var(--muted);
  font-size: 12px;
  font-weight: 700;
}

.staff-event-card {
  display: grid;
  grid-template-columns: 48px minmax(0, 1fr) 110px auto;
  align-items: center;
  gap: 20px;
  padding: 18px 20px;
  border: 1px solid var(--line);
  border-radius: 17px;
  background: white;
}

.staff-number {
  color: #b7bac0;
  font-size: 11px;
  font-weight: 800;
}

.staff-event-info h3 {
  margin: 8px 0 5px;
  font-size: 18px;
}

.staff-event-info p {
  margin: 0;
  color: var(--muted);
  font-size: 12px;
}

.staff-limit {
  text-align: center;
}

.staff-limit strong {
  font-family: "Manrope", sans-serif;
  font-size: 25px;
}

.staff-actions {
  display: flex;
  gap: 7px;
}

.staff-actions button {
  padding: 9px 12px;
  border: 1px solid var(--line);
  border-radius: 9px;
  color: var(--ink);
  background: white;
  font-size: 11px;
  font-weight: 800;
}

.staff-actions button:hover {
  border-color: #e2aeb2;
  color: var(--red-700);
}

.staff-actions .danger {
  color: #9a1820;
}

.modal-backdrop {
  position: fixed;
  inset: 0;
  z-index: 100;
  display: grid;
  place-items: center;
  padding: 20px;
  background: rgba(17, 18, 21, 0.52);
  backdrop-filter: blur(7px);
}

.event-modal {
  width: min(680px, 100%);
  max-height: calc(100vh - 40px);
  overflow: auto;
  padding: 27px;
  border-radius: 24px;
  background: white;
  box-shadow: 0 35px 100px rgba(0,0,0,.24);
}

.modal-header {
  display: flex;
  justify-content: space-between;
  gap: 20px;
  margin-bottom: 25px;
}

.modal-header h2 {
  margin: 8px 0 0;
  font-size: 29px;
  letter-spacing: -.04em;
}

.modal-close {
  width: 38px;
  height: 38px;
  border: 1px solid var(--line);
  border-radius: 11px;
  color: #55585f;
  background: white;
  font-size: 22px;
}

.staff-form {
  display: grid;
  gap: 17px;
}

.modal-actions {
  display: flex;
  justify-content: flex-end;
  gap: 9px;
  margin-top: 5px;
}

/* RESPONSIVE */

@media (max-width: 900px) {
  .home-hero-grid,
  .auth-shell,
  .detail-grid {
    grid-template-columns: 1fr;
  }

  .home-hero {
    min-height: auto;
  }

  .home-hero-grid {
    min-height: auto;
    padding-top: 80px;
    padding-bottom: 80px;
  }

  .hero-visual {
    min-height: 430px;
  }

  .auth-brand {
    min-height: 420px;
  }

  .auth-form-side {
    padding: 60px 30px;
  }

  .detail-grid {
    gap: 45px;
  }

  .registration-card-panel {
    position: static;
  }

  .stats-grid,
  .staff-stats {
    grid-template-columns: repeat(2, 1fr);
  }

  .staff-event-card {
    grid-template-columns: 42px minmax(0, 1fr) auto;
  }

  .staff-limit {
    display: none;
  }
}

@media (max-width: 720px) {
  .page-container,
  .dashboard-shell,
  .staff-stats {
    width: min(100% - 28px, 1180px);
  }

  .app-header {
    min-height: 68px;
    padding: 0 14px;
  }

  .brand-name {
    font-size: 13px;
  }

  .brand-mark {
    width: 36px;
    height: 36px;
  }

  .header-actions {
    gap: 2px;
  }

  .header-link {
    display: none;
  }

  .header-cta {
    padding: 9px 12px;
    font-size: 12px;
  }

  .home-copy h1,
  .events-hero h1 {
    font-size: 48px;
  }

  .home-copy p,
  .events-hero p {
    font-size: 15px;
  }

  .hero-buttons {
    display: grid;
  }

  .hero-buttons .btn {
    width: 100%;
  }

  .hero-visual {
    min-height: 390px;
  }

  .event-poster {
    width: 260px;
    min-height: 340px;
  }

  .poster-day {
    font-size: 105px;
  }

  .floating-events {
    right: 0;
  }

  .floating-new {
    left: 0;
  }

  .stats-strip {
    grid-template-columns: 1fr 1fr;
  }

  .stats-strip > div {
    border-bottom: 1px solid var(--line);
  }

  .stats-strip > div:nth-child(odd) {
    border-left: 1px solid var(--line);
  }

  .home-events,
  .events-browser {
    padding-top: 70px;
    padding-bottom: 75px;
  }

  .section-heading,
  .dashboard-hero,
  .staff-hero,
  .cta-inner {
    align-items: flex-start;
    flex-direction: column;
  }

  .home-event-grid,
  .event-grid {
    grid-template-columns: 1fr;
  }

  .events-hero {
    padding: 70px 0;
  }

  .events-hero-grid {
    grid-template-columns: 1fr;
  }

  .hero-counter {
    align-items: center;
  }

  .hero-counter strong {
    font-size: 68px;
  }

  .event-card {
    min-height: 360px;
  }

  .event-meta {
    grid-template-columns: 1fr;
  }

  .detail-hero {
    padding: 55px 0 65px;
  }

  .detail-hero-content {
    margin-top: 45px;
  }

  .detail-hero h1 {
    font-size: 43px;
  }

  .info-grid,
  .field-grid {
    grid-template-columns: 1fr;
  }

  .dashboard-shell {
    padding-top: 45px;
  }

  .stats-grid,
  .staff-stats {
    grid-template-columns: 1fr 1fr;
  }

  .registration-card {
    grid-template-columns: 70px minmax(0, 1fr);
  }

  .registration-card .card-link {
    grid-column: 2;
    padding: 0;
  }

  .date-tile {
    min-height: 75px;
  }

  .staff-event-card {
    grid-template-columns: 35px minmax(0, 1fr);
  }

  .staff-actions {
    grid-column: 2;
  }

  .staff-actions button {
    flex: 1;
  }

  .auth-brand {
    min-height: 350px;
    padding: 28px 22px;
  }

  .auth-brand-copy h1 {
    font-size: 45px;
  }

  .auth-brand-footer {
    margin-top: 45px;
  }

  .auth-form-side {
    padding: 45px 20px;
  }

  .auth-form-box h2 {
    font-size: 31px;
  }

  .auth-mobile-logo {
    display: grid;
    place-items: center;
    width: 43px;
    height: 43px;
    margin-bottom: 30px;
    border-radius: 13px;
    color: white;
    background: var(--red-700);
    font-family: "Manrope", sans-serif;
    font-weight: 800;
  }

  .footer-inner {
    align-items: flex-start;
    flex-direction: column;
  }

  .cta-button {
    min-width: 130px;
    min-height: 130px;
  }
}

@media (max-width: 460px) {
  .stats-grid,
  .staff-stats {
    grid-template-columns: 1fr;
  }

  .profile-mini {
    display: none;
  }

  .dashboard-hero h1 {
    font-size: 40px;
  }

  .registration-card {
    grid-template-columns: 1fr;
  }

  .registration-card .card-link {
    grid-column: auto;
  }

  .date-tile {
    width: 82px;
  }

  .event-poster {
    width: 230px;
  }

  .floating-card {
    transform: scale(.9);
  }

  .floating-events {
    right: -12px;
  }

  .floating-new {
    left: -12px;
  }

  .modal-actions {
    flex-direction: column-reverse;
  }

  .modal-actions .btn {
    width: 100%;
  }
}



===== FILE: E:\School Event Planner\frontend\src\main.jsx =====

import React from "react";
import ReactDOM from "react-dom/client";

import App from "./App";
import "./index.css";


ReactDOM.createRoot(
  document.getElementById("root")
).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);


===== FILE: E:\School Event Planner\frontend\src\components\Layout.jsx =====

import Navbar from "./Navbar";


function Layout({ children }) {
  return (
    <div className="app-layout">
      <Navbar />

      <main className="main-content">
        {children}
      </main>

      <footer className="footer">
        <div className="footer-inner">
          <strong>School Event Planner</strong>

          <span>
            Platform kegiatan dan perlombaan sekolah.
          </span>
        </div>
      </footer>
    </div>
  );
}

export default Layout;


===== FILE: E:\School Event Planner\frontend\src\components\Navbar.jsx =====

import { NavLink, Link } from "react-router-dom";


function Navbar() {
  return (
    <header className="navbar">
      <div className="navbar-inner">
        <Link to="/" className="navbar-brand">
  <span className="brand-mark">SE</span>

  <span>
    <strong>School Event</strong>
    <small>Planner</small>
  </span>
</Link>

        <nav className="navbar-links">
          <NavLink to="/">Beranda</NavLink>

          <NavLink to="/events">
            Lomba
          </NavLink>

          <NavLink to="/announcements">
            Pengumuman
          </NavLink>
        </nav>

        <div className="navbar-actions">
          <Link
            to="/login"
            className="navbar-login"
          >
            Masuk
          </Link>

          <Link
            to="/register"
            className="navbar-register"
          >
            Daftar
          </Link>
        </div>
      </div>
    </header>
  );
}

export default Navbar;


===== FILE: E:\School Event Planner\frontend\src\pages\Announcements.jsx =====

import { useEffect, useState } from "react";
import { Link } from "react-router-dom";
import "../styles/Announcements.css";

const API_URL =
  import.meta.env.VITE_API_URL ||
  "http://127.0.0.1:8000";

function formatDate(isoString) {
  if (!isoString) return "";

  try {
    return new Date(isoString).toLocaleDateString("id-ID", {
      day: "numeric",
      month: "long",
      year: "numeric",
    });
  } catch {
    return isoString;
  }
}

function Announcements() {
  const [announcements, setAnnouncements] = useState([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState("");

  useEffect(() => {
    async function loadAnnouncements() {
      try {
        setLoading(true);
        setError("");

        const response = await fetch(`${API_URL}/announcements`);
        const data = await response.json();

        if (!response.ok) {
          throw new Error(
            data.detail || "Gagal mengambil pengumuman."
          );
        }

        setAnnouncements(Array.isArray(data) ? data : []);
      } catch (err) {
        setError(
          err.message || "Gagal mengambil pengumuman."
        );
      } finally {
        setLoading(false);
      }
    }

    loadAnnouncements();
  }, []);

  return (
    <main className="announcements-page">
      <header className="announcements-header">
        <div className="announcements-container announcements-header-inner">
          <Link to="/" className="announcements-brand">
            <span className="announcements-brand-mark">SE</span>

            <span className="announcements-brand-name">
              School Event
              <strong>Planner</strong>
            </span>
          </Link>

          <nav className="announcements-nav">
            <Link to="/" className="announcements-nav-link">
              Beranda
            </Link>

            <Link to="/events" className="announcements-nav-link">
              Kegiatan
            </Link>

            <Link
              to="/announcements"
              className="announcements-nav-link active"
            >
              Pengumuman
            </Link>

            <Link to="/login" className="announcements-nav-link">
              Masuk
            </Link>

            <Link
              to="/register"
              className="announcements-nav-button"
            >
              Daftar
            </Link>
          </nav>
        </div>
      </header>

      <section className="announcements-hero">
        <div className="announcements-container">
          <span className="announcements-eyebrow">
            INFORMASI TERBARU
          </span>

          <h1>
            Pengumuman
            <span> kegiatan sekolah.</span>
          </h1>

          <p>
            Ikuti kabar terbaru seputar kegiatan, jadwal, dan
            perubahan penting dari panitia.
          </p>
        </div>
      </section>

      <section className="announcements-content">
        <div className="announcements-container">
          {loading && (
            <div className="announcements-state">
              <div className="announcements-spinner" />
              <strong>Memuat pengumuman</strong>
              <span>Menyiapkan informasi terbaru...</span>
            </div>
          )}

          {!loading && error && (
            <div className="announcements-state error">
              <span className="announcements-state-label">
                ERROR
              </span>
              <strong>Pengumuman gagal dimuat.</strong>
              <p>{error}</p>
            </div>
          )}

          {!loading && !error && announcements.length === 0 && (
            <div className="announcements-state empty">
              <div className="announcements-empty-icon">â€”</div>
              <span className="announcements-state-label">
                BELUM ADA PENGUMUMAN
              </span>
              <strong>Belum ada pengumuman saat ini.</strong>
              <p>Pengumuman baru akan muncul di sini ketika tersedia.</p>
            </div>
          )}

          {!loading && !error && announcements.length > 0 && (
            <div className="announcements-list">
              {announcements.map((item, index) => (
                <article className="announcement-card" key={item.id}>
                  <div className="announcement-number">
                    {String(index + 1).padStart(2, "0")}
                  </div>

                  <div className="announcement-main">
                    <span className="announcement-date">
                      {formatDate(item.created_at)}
                    </span>

                    <h2>{item.title}</h2>

                    <p>{item.content}</p>
                  </div>
                </article>
              ))}
            </div>
          )}
        </div>
      </section>

      <footer className="announcements-footer">
        <div className="announcements-container announcements-footer-inner">
          <div>
            <strong>School Event Planner</strong>
            <span>Platform kegiatan sekolah</span>
          </div>

          <span>Â© 2026 School Event Planner</span>
        </div>
      </footer>
    </main>
  );
}

export default Announcements;


===== FILE: E:\School Event Planner\frontend\src\pages\Dashboard.jsx =====

import { useEffect, useState } from "react";
import { Link, useNavigate } from "react-router-dom";
import "../styles/Dashboard.css";

const API_URL =
  import.meta.env.VITE_API_URL ||
  "http://127.0.0.1:8000";

function Dashboard() {
  const navigate = useNavigate();

  const [user, setUser] = useState(null);
  const [registrations, setRegistrations] = useState([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState("");

  useEffect(() => {
    async function loadDashboard() {
      const token = localStorage.getItem("access_token");

      if (!token) {
        navigate("/login");
        return;
      }

      try {
        const headers = {
          Authorization: `Bearer ${token}`,
        };

        const [userResponse, registrationsResponse] =
          await Promise.all([
            fetch(`${API_URL}/auth/me`, { headers }),
            fetch(`${API_URL}/registrations/me`, { headers }),
          ]);

        const userData = await userResponse.json();
        const registrationData =
          await registrationsResponse.json();

        if (!userResponse.ok) {
          throw new Error(
            userData.detail || "Sesi login tidak valid."
          );
        }

        setUser(userData);

        if (registrationsResponse.ok) {
          setRegistrations(
            Array.isArray(registrationData)
              ? registrationData
              : registrationData.registrations || []
          );
        }
      } catch (err) {
        setError(
          err.message || "Gagal mengambil data dashboard."
        );
      } finally {
        setLoading(false);
      }
    }

    loadDashboard();
  }, [navigate]);

  function handleLogout() {
    localStorage.removeItem("access_token");
    navigate("/login");
  }

  if (loading) {
    return (
      <main className="dashboard-page">
        <div className="dashboard-state">
          <div className="dashboard-spinner" />
          <strong>Memuat dashboard</strong>
          <span>Menyiapkan data akunmu...</span>
        </div>
      </main>
    );
  }

  const approvedCount = registrations.filter(
    (item) => item.status === "approved"
  ).length;

  const pendingCount = registrations.filter(
    (item) => item.status === "pending"
  ).length;

  return (
    <main className="dashboard-page">
      <header className="dashboard-header">
        <div className="dashboard-header-inner">
          <Link to="/" className="dashboard-brand">
            <span className="dashboard-brand-mark">SE</span>

            <span>
              <strong>School Event</strong>
              <small>Planner</small>
            </span>
          </Link>

          <nav className="dashboard-nav">
            <Link to="/events">Kegiatan</Link>

            <button
              type="button"
              onClick={handleLogout}
              className="dashboard-logout"
            >
              Keluar
            </button>
          </nav>
        </div>
      </header>

      <section className="dashboard-shell">
        <div className="dashboard-hero">
          <div>
            <span className="dashboard-eyebrow">
              PESERTA
            </span>

            <h1>
              Halo,{" "}
              <span>
                {user?.name || "Peserta"}
              </span>
            </h1>

            <p>
              Pantau kegiatan dan pendaftaranmu di sini.
            </p>
          </div>

          <div className="dashboard-profile">
            <div className="dashboard-avatar">
              {(user?.name || "P")
                .charAt(0)
                .toUpperCase()}
            </div>

            <div>
              <strong>
                {user?.name || "Peserta"}
              </strong>
              <span>
                {user?.email || "-"}
              </span>
            </div>
          </div>
        </div>

        {error && (
          <div className="dashboard-alert">
            <strong>!</strong>
            <span>{error}</span>
          </div>
        )}

        <div className="dashboard-stats">
          <div className="dashboard-stat dashboard-stat-accent">
            <span>TOTAL PENDAFTARAN</span>
            <strong>{registrations.length}</strong>
            <small>Kegiatan yang kamu ikuti</small>
          </div>

          <div className="dashboard-stat">
            <span>DISETUJUI</span>
            <strong>{approvedCount}</strong>
            <small>Pendaftaran diterima</small>
          </div>

          <div className="dashboard-stat">
            <span>MENUNGGU</span>
            <strong>{pendingCount}</strong>
            <small>Masih dalam proses</small>
          </div>

          <div className="dashboard-stat">
            <span>STATUS</span>
            <strong className="dashboard-active">
              AKTIF
            </strong>
            <small>Akun peserta</small>
          </div>
        </div>

        <section className="dashboard-section">
          <div className="dashboard-section-heading">
            <div>
              <span className="dashboard-eyebrow">
                AKTIVITAS
              </span>
              <h2>Pendaftaran saya</h2>
            </div>

            <Link to="/events">
              Jelajahi kegiatan â†—
            </Link>
          </div>

          {registrations.length === 0 ? (
            <div className="dashboard-empty">
              <div className="dashboard-empty-icon">
                +
              </div>

              <span className="dashboard-eyebrow">
                BELUM ADA PENDAFTARAN
              </span>

              <h3>
                Kamu belum mengikuti kegiatan.
              </h3>

              <p>
                Jelajahi kegiatan sekolah dan temukan
                sesuatu yang menarik untuk diikuti.
              </p>

              <Link
                to="/events"
                className="dashboard-primary-button"
              >
                Lihat kegiatan
              </Link>
            </div>
          ) : (
            <div className="registration-list">
              {registrations.map((registration, index) => {
                const event =
                  registration.event || registration;

                const status =
                  registration.status || "pending";

                return (
                  <article
                    key={
                      registration.id ||
                      registration.event_id ||
                      index
                    }
                    className="registration-card"
                  >
                    <div className="registration-number">
                      {String(index + 1).padStart(2, "0")}
                    </div>

                    <div className="registration-main">
                      <span className="registration-category">
                        {event.category === "team"
                          ? "TIM"
                          : "INDIVIDU"}
                      </span>

                      <h3>
                        {event.name ||
                          registration.event_name ||
                          "Kegiatan"}
                      </h3>

                      <p>
                        {event.date ||
                          registration.date ||
                          "Tanggal belum tersedia"}
                      </p>
                    </div>

                    <span
                      className={`registration-status ${status}`}
                    >
                      {status === "approved"
                        ? "Disetujui"
                        : status === "pending"
                          ? "Menunggu"
                          : status === "closed"
                            ? "Ditutup"
                            : status}
                    </span>

                    {event.id && (
                      <Link
                        to={`/events/${event.id}`}
                        className="registration-link"
                      >
                        Detail â†—
                      </Link>
                    )}
                  </article>
                );
              })}
            </div>
          )}
        </section>
      </section>

      <footer className="dashboard-footer">
        <div>
          <strong>School Event Planner</strong>
          <span>
            Platform kegiatan dan perlombaan sekolah.
          </span>
        </div>

        <span>&copy; 2026</span>
      </footer>
    </main>
  );
}

export default Dashboard;


===== FILE: E:\School Event Planner\frontend\src\pages\EventDetail.jsx =====

import { useEffect, useState } from "react";
import { Link, useNavigate, useParams } from "react-router-dom";
import { getEvent, registerEvent } from "../services/api";
import "../styles/EventDetail.css";

function EventDetail() {
  const { id } = useParams();
  const navigate = useNavigate();

  const [event, setEvent] = useState(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState("");
  const [registering, setRegistering] = useState(false);
  const [success, setSuccess] = useState("");

  const isLoggedIn = Boolean(
    localStorage.getItem("access_token")
  );

  useEffect(() => {
    async function loadEvent() {
      try {
        setLoading(true);
        setError("");

        const data = await getEvent(id);
        setEvent(data);
      } catch (err) {
        setError(
          err.message || "Gagal mengambil detail kegiatan."
        );
      } finally {
        setLoading(false);
      }
    }

    loadEvent();
  }, [id]);

  async function handleRegister() {
    if (!isLoggedIn) {
      navigate("/login");
      return;
    }

    try {
      setRegistering(true);
      setError("");
      setSuccess("");

      await registerEvent(id);

      setSuccess("Pendaftaran kegiatan berhasil.");
    } catch (err) {
      if (err.message === "LOGIN_REQUIRED") {
        navigate("/login");
        return;
      }

      setError(
        err.message || "Gagal mendaftar kegiatan."
      );
    } finally {
      setRegistering(false);
    }
  }

  if (loading) {
    return (
      <main className="event-detail-page">
        <header className="detail-header">
          <div className="detail-container detail-header-inner">
            <Link to="/" className="detail-brand">
              <span className="detail-brand-mark">SE</span>

              <span className="detail-brand-name">
                School Event
                <strong>Planner</strong>
              </span>
            </Link>

            <Link to="/events" className="detail-back">
              â† Kembali
            </Link>
          </div>
        </header>

        <div className="detail-state">
          <div className="detail-spinner" />
          <strong>Memuat kegiatan</strong>
          <span>Menyiapkan detail kegiatan...</span>
        </div>
      </main>
    );
  }

  if (error && !event) {
    return (
      <main className="event-detail-page">
        <header className="detail-header">
          <div className="detail-container detail-header-inner">
            <Link to="/" className="detail-brand">
              <span className="detail-brand-mark">SE</span>

              <span className="detail-brand-name">
                School Event
                <strong>Planner</strong>
              </span>
            </Link>

            <Link to="/events" className="detail-back">
              â† Kembali
            </Link>
          </div>
        </header>

        <div className="detail-state error">
          <span className="detail-state-label">
            ERROR
          </span>

          <strong>Kegiatan tidak ditemukan.</strong>

          <p>{error}</p>

          <Link
            to="/events"
            className="detail-state-button"
          >
            Kembali ke kegiatan
          </Link>
        </div>
      </main>
    );
  }

  const isTeam = event.category === "team";
  const isUpcoming = event.status === "upcoming";

  return (
    <main className="event-detail-page">
      {/* HEADER */}
      <header className="detail-header">
        <div className="detail-container detail-header-inner">
          <Link to="/" className="detail-brand">
            <span className="detail-brand-mark">SE</span>

            <span className="detail-brand-name">
              School Event
              <strong>Planner</strong>
            </span>
          </Link>

          <nav className="detail-nav">
            <Link to="/" className="detail-nav-link">
              Beranda
            </Link>

            <Link
              to="/events"
              className="detail-nav-link active"
            >
              Kegiatan
            </Link>

            {isLoggedIn ? (
              <Link
                to="/dashboard"
                className="detail-nav-button"
              >
                Dashboard
              </Link>
            ) : (
              <>
                <Link
                  to="/login"
                  className="detail-nav-link"
                >
                  Masuk
                </Link>

                <Link
                  to="/register"
                  className="detail-nav-button"
                >
                  Daftar
                </Link>
              </>
            )}
          </nav>
        </div>
      </header>

      {/* HERO */}
      <section className="detail-hero">
        <div className="detail-container">
          <Link
            to="/events"
            className="detail-back"
          >
            â† Semua kegiatan
          </Link>

          <div className="detail-hero-content">
            <div className="detail-tag-row">
              <span className="detail-tag">
                {isTeam ? "TIM" : "INDIVIDU"}
              </span>

              {event.status && (
                <span className="detail-status">
                  {event.status === "upcoming"
                    ? "AKAN DATANG"
                    : event.status.toUpperCase()}
                </span>
              )}
            </div>

            <h1>{event.name}</h1>

            <p>
              {event.description ||
                "Informasi lengkap mengenai kegiatan sekolah."}
            </p>
          </div>
        </div>
      </section>

      {/* CONTENT */}
      <section className="detail-content">
        <div className="detail-container detail-layout">
          <article className="detail-main">
            <span className="detail-eyebrow">
              DETAIL KEGIATAN
            </span>

            <h2>Tentang kegiatan</h2>

            <p className="detail-description">
              {event.description ||
                "Belum ada deskripsi kegiatan."}
            </p>

            <div className="detail-info-grid">
              <div className="detail-info-card">
                <span>TANGGAL</span>
                <strong>{event.date || "-"}</strong>
              </div>

              <div className="detail-info-card">
                <span>WAKTU</span>
                <strong>
                  {event.start_time || "-"} â€“{" "}
                  {event.end_time || "-"}
                </strong>
              </div>

              <div className="detail-info-card">
                <span>LOKASI</span>
                <strong>{event.location || "-"}</strong>
              </div>

              <div className="detail-info-card">
                <span>KATEGORI</span>
                <strong>
                  {isTeam
                    ? "Perlombaan Tim"
                    : "Individu"}
                </strong>
              </div>

              {event.max_participants && (
                <div className="detail-info-card">
                  <span>KUOTA</span>
                  <strong>
                    {event.max_participants} peserta
                  </strong>
                </div>
              )}

              {event.registration_deadline && (
                <div className="detail-info-card">
                  <span>PENDAFTARAN</span>
                  <strong>
                    {event.registration_deadline}
                  </strong>
                </div>
              )}
            </div>
          </article>

          {/* REGISTRATION CARD */}
          <aside className="detail-registration-card">
            <div className="detail-availability">
              <span
                className={
                  isUpcoming
                    ? "detail-dot open"
                    : "detail-dot"
                }
              />

              {isUpcoming
                ? "Pendaftaran tersedia"
                : "Pendaftaran ditutup"}
            </div>

            <span className="detail-card-label">
              PENDAFTARAN
            </span>

            <h2>
              Siap ikut
              <br />
              kegiatan ini?
            </h2>

            <p>
              Daftarkan dirimu dan ikuti kegiatan
              sekolah sesuai jadwal yang tersedia.
            </p>

            {error && (
              <div className="detail-alert">
                {error}
              </div>
            )}

            {success ? (
              <div className="detail-success">
                <div className="detail-success-icon">
                  âœ“
                </div>

                <strong>
                  Pendaftaran berhasil
                </strong>

                <span>{success}</span>

                <Link
                  to="/dashboard"
                  className="detail-primary-button"
                >
                  Lihat dashboard
                </Link>
              </div>
            ) : (
              <>
                <button
                  type="button"
                  className="detail-primary-button"
                  onClick={handleRegister}
                  disabled={
                    !isUpcoming || registering
                  }
                >
                  {registering
                    ? "Mendaftarkan..."
                    : !isLoggedIn
                    ? "Masuk untuk mendaftar"
                    : "Daftar kegiatan"}
                </button>

                <span className="detail-note">
                  {isLoggedIn
                    ? "Pendaftaran akan tercatat pada akunmu."
                    : "Kamu perlu masuk ke akun terlebih dahulu untuk melakukan pendaftaran."}
                </span>
              </>
            )}
          </aside>
        </div>
      </section>

      {/* FOOTER */}
      <footer className="detail-footer">
        <div className="detail-container detail-footer-inner">
          <div>
            <strong>
              School Event Planner
            </strong>

            <span>
              Platform kegiatan sekolah
            </span>
          </div>

          <span>
            Â© 2026 School Event Planner
          </span>
        </div>
      </footer>
    </main>
  );
}

export default EventDetail;


===== FILE: E:\School Event Planner\frontend\src\pages\Events.jsx =====

import { useEffect, useState } from "react";
import { Link } from "react-router-dom";
import { getEvents } from "../services/api";
import "../styles/Events.css";

function Events() {
  const [events, setEvents] = useState([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState("");
  const [filter, setFilter] = useState("all");

  const token = localStorage.getItem("access_token");

  let user = null;

  try {
    user = JSON.parse(
      localStorage.getItem("user") || "null"
    );
  } catch {
    user = null;
  }

  const isLoggedIn = Boolean(token);

  const isCommittee =
    user?.role === "committee" ||
    user?.role === "staff";

  const dashboardPath = isCommittee
    ? "/staff"
    : "/dashboard";

  const dashboardLabel = isCommittee
    ? "Panel Panitia"
    : "Dashboard";

  useEffect(() => {
    async function loadEvents() {
      try {
        setLoading(true);
        setError("");

        const data = await getEvents();

        setEvents(
          Array.isArray(data) ? data : []
        );
      } catch (err) {
        setError(
          err.message ||
            "Gagal mengambil kegiatan."
        );
      } finally {
        setLoading(false);
      }
    }

    loadEvents();
  }, []);

  const filteredEvents = events.filter(
    (event) => {
      if (filter === "all") {
        return true;
      }

      return event.category === filter;
    }
  );

  return (
    <main className="page events-page">
      {/* HEADER */}
      <header className="events-header">
        <div className="events-container events-header-inner">
          <Link
            to="/"
            className="events-brand"
          >
            <span className="events-brand-mark">
              SE
            </span>

            <span className="events-brand-name">
              School Event
              <strong>Planner</strong>
            </span>
          </Link>

          <nav className="events-nav">
            <Link
              to="/events"
              className="active"
            >
              Kegiatan
            </Link>

            {isLoggedIn ? (
              <Link
                to={dashboardPath}
                className="events-nav-cta"
              >
                {dashboardLabel}
              </Link>
            ) : (
              <>
                <Link to="/login">
                  Masuk
                </Link>

                <Link
                  to="/register"
                  className="events-nav-cta"
                >
                  Daftar
                </Link>
              </>
            )}
          </nav>
        </div>
      </header>

      {/* HERO */}
      <section className="events-hero">
        <div className="events-container events-hero-grid">
          <div>
            <span className="events-eyebrow events-eyebrow-light">
              SCHOOL EVENTS 2026
            </span>

            <h1>
              Temukan kegiatan
              <br />
              <span>untukmu.</span>
            </h1>

            <p>
              Jelajahi berbagai lomba dan kegiatan
              sekolah yang tersedia. Pilih yang kamu
              suka, lalu daftar dalam beberapa langkah.
            </p>
          </div>

          <div className="events-hero-counter">
            <strong>
              {events.length}
            </strong>

            <span>
              kegiatan
              <br />
              tersedia
            </span>
          </div>
        </div>
      </section>

      {/* EVENTS BROWSER */}
      <section className="events-container events-browser">
        <div className="events-section-heading">
          <div>
            <span className="events-eyebrow">
              EXPLORE
            </span>

            <h2>
              Semua kegiatan
            </h2>
          </div>

          <div className="events-filter-group">
            {[
              ["all", "Semua"],
              ["individual", "Individu"],
              ["team", "Tim"],
            ].map(([value, label]) => (
              <button
                key={value}
                type="button"
                className={
                  filter === value
                    ? "events-filter active"
                    : "events-filter"
                }
                onClick={() =>
                  setFilter(value)
                }
              >
                {label}
              </button>
            ))}
          </div>
        </div>

        {/* LOADING */}
        {loading && (
          <div className="events-state">
            <div className="events-spinner" />

            <strong>
              Memuat kegiatan
            </strong>

            <span>
              Menyiapkan daftar kegiatan...
            </span>
          </div>
        )}

        {/* ERROR */}
        {!loading && error && (
          <div className="events-state events-state-error">
            <span className="events-eyebrow">
              ERROR
            </span>

            <h3>
              Data kegiatan gagal dimuat.
            </h3>

            <p>
              {error}
            </p>
          </div>
        )}

        {/* EMPTY */}
        {!loading &&
          !error &&
          filteredEvents.length === 0 && (
            <div className="events-state events-state-empty">
              <div className="events-empty-icon">
                â€”
              </div>

              <span className="events-eyebrow">
                TIDAK ADA DATA
              </span>

              <h3>
                Tidak ada kegiatan pada filter ini.
              </h3>

              <p>
                Coba pilih kategori lainnya.
              </p>
            </div>
          )}

        {/* EVENTS */}
        {!loading &&
          !error &&
          filteredEvents.length > 0 && (
            <div className="events-grid">
              {filteredEvents.map(
                (event, index) => (
                  <Link
                    key={event.id}
                    to={`/events/${event.id}`}
                    className="events-card"
                  >
                    <div className="events-card-top">
                      <span className="events-tag">
                        {event.category === "team"
                          ? "TIM"
                          : "INDIVIDU"}
                      </span>

                      <span className="events-number">
                        {String(index + 1).padStart(
                          2,
                          "0"
                        )}
                      </span>
                    </div>

                    <div className="events-card-body">
                      <h3>
                        {event.name}
                      </h3>

                      <p>
                        {event.description}
                      </p>
                    </div>

                    <div className="events-meta">
                      <div>
                        <span>
                          TANGGAL
                        </span>

                        <strong>
                          {event.date}
                        </strong>
                      </div>

                      <div>
                        <span>
                          WAKTU
                        </span>

                        <strong>
                          {event.start_time}
                          {" â€“ "}
                          {event.end_time}
                        </strong>
                      </div>

                      <div>
                        <span>
                          LOKASI
                        </span>

                        <strong>
                          {event.location}
                        </strong>
                      </div>
                    </div>

                    <div className="events-card-arrow">
                      â†—
                    </div>
                  </Link>
                )
              )}
            </div>
          )}
      </section>
    </main>
  );
}

export default Events;


===== FILE: E:\School Event Planner\frontend\src\pages\Home.jsx =====

import { useEffect, useState } from "react";
import { Link } from "react-router-dom";
import { getEvents } from "../services/api";
import "../styles/Home.css";

function Home() {
  const [events, setEvents] = useState([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState("");

  const isLoggedIn = Boolean(
    localStorage.getItem("access_token")
  );

  useEffect(() => {
    async function loadEvents() {
      try {
        const data = await getEvents();
        setEvents(Array.isArray(data) ? data : []);
      } catch (err) {
        setError(
          err.message || "Gagal mengambil data kegiatan."
        );
      } finally {
        setLoading(false);
      }
    }

    loadEvents();
  }, []);

  const upcomingEvents = events
    .filter((event) => event.status === "upcoming")
    .slice(0, 3);

  return (
    <main className="home-page">
      {/* HEADER */}
      <header className="home-header">
        <div className="home-container home-header-inner">
          <Link to="/" className="home-brand">
            <span className="home-brand-mark">SE</span>

            <span className="home-brand-name">
              School Event
              <strong>Planner</strong>
            </span>
          </Link>

          <nav className="home-nav">
  <Link to="/events">Kegiatan</Link>
  <Link to="/announcements">Pengumuman</Link>

  {isLoggedIn ? (
    <button
      className="home-nav-logout"
      onClick={() => {
        localStorage.removeItem("access_token");
        window.location.href = "/login";
      }}
    >
      Keluar
    </button>
  ) : (
    <>
      <Link to="/login">Masuk</Link>

      <Link
        to="/register"
        className="home-nav-cta"
      >
        Daftar
      </Link>
    </>
  )}
</nav>
        </div>
      </header>

      {/* HERO */}
      <section className="home-hero">
        <div className="home-container home-hero-grid">
          <div className="home-hero-content">
            <span className="home-eyebrow home-eyebrow-light">
              PLATFORM KEGIATAN SEKOLAH
            </span>

            <h1>
              Semua kegiatan.
              <br />
              <span>Satu tempat.</span>
            </h1>

            <p>
              Temukan lomba, lihat jadwal, daftar kegiatan, dan
              pantau pendaftaranmu dalam satu platform yang
              sederhana.
            </p>

            <div className="home-hero-actions">
              <Link
                to="/events"
                className="home-btn home-btn-white"
              >
                Jelajahi kegiatan
                <span>â†—</span>
              </Link>

              {isLoggedIn ? (
                <Link
                  to="/dashboard"
                  className="home-btn home-btn-outline"
                >
                  Buka dashboard
                </Link>
              ) : (
                <Link
                  to="/register"
                  className="home-btn home-btn-outline"
                >
                  Buat akun
                </Link>
              )}
            </div>

            <div className="home-trust">
              <span className="home-pulse-dot" />
              Terbuka untuk seluruh peserta sekolah
            </div>
          </div>

          {/* HERO VISUAL */}
          <div className="home-hero-visual">
            <div className="home-orb home-orb-one" />
            <div className="home-orb home-orb-two" />

            <div className="home-event-poster">
              <div className="home-poster-top">
                <span>SE Â· 2026</span>
                <span className="home-poster-status">
                  AKTIF
                </span>
              </div>

              <div className="home-poster-day">
                17
              </div>

              <div className="home-poster-title">
                <span>AGUSTUS</span>

                <strong>
                  Semarak
                  <br />
                  Kegiatan Sekolah
                </strong>
              </div>

              <div className="home-poster-bottom">
                <span>Lomba & kegiatan</span>
                <span>2026 â†—</span>
              </div>
            </div>

            <div className="home-floating-card home-floating-events">
              <strong>
                {events.length || "â€”"}
              </strong>

              <span>
                kegiatan
                <br />
                tersedia
              </span>
            </div>

            <div className="home-floating-card home-floating-new">
              <span className="home-floating-plus">
                +
              </span>

              <div>
                <small>Kegiatan baru</small>
                <strong>Segera hadir</strong>
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* STATS */}
      <section className="home-stats">
        <div className="home-container home-stats-grid">
          <div>
            <strong>{events.length}</strong>
            <span>Total kegiatan</span>
          </div>

          <div>
            <strong>
              {upcomingEvents.length}
            </strong>
            <span>Kegiatan mendatang</span>
          </div>

          <div>
            <strong>01</strong>
            <span>Platform terpusat</span>
          </div>

          <div>
            <strong>âˆž</strong>
            <span>Kesempatan ikut</span>
          </div>
        </div>
      </section>

      {/* EVENTS */}
      <section className="home-container home-events">
        <div className="home-section-heading">
          <div>
            <span className="home-eyebrow">
              KEGIATAN TERBARU
            </span>

            <h2>
              Temukan kegiatan yang menarik untukmu.
            </h2>
          </div>

          <Link
            to="/events"
            className="home-inline-link"
          >
            Lihat semua â†—
          </Link>
        </div>

        {loading && (
          <div className="home-state">
            <div className="home-spinner" />

            <strong>Memuat kegiatan</strong>

            <span>
              Menyiapkan kegiatan terbaru...
            </span>
          </div>
        )}

        {!loading && error && (
          <div className="home-state home-state-error">
            <span className="home-eyebrow">
              ERROR
            </span>

            <h3>Gagal memuat kegiatan.</h3>

            <p>{error}</p>
          </div>
        )}

        {!loading &&
          !error &&
          upcomingEvents.length === 0 && (
            <div className="home-state">
              <span className="home-eyebrow">
                BELUM TERSEDIA
              </span>

              <h3>
                Belum ada kegiatan mendatang.
              </h3>

              <p>
                Kegiatan baru akan muncul di sini ketika
                tersedia.
              </p>
            </div>
          )}

        {!loading &&
          !error &&
          upcomingEvents.length > 0 && (
            <div className="home-event-grid">
              {upcomingEvents.map((event, index) => (
                <Link
                  key={event.id}
                  to={`/events/${event.id}`}
                  className="home-event-card"
                >
                  <div className="home-event-card-top">
                    <span className="home-tag">
                      {event.category === "team"
                        ? "TIM"
                        : "INDIVIDU"}
                    </span>

                    <span className="home-event-arrow">
                      â†—
                    </span>
                  </div>

                  <span className="home-event-index">
                    {String(index + 1).padStart(2, "0")}
                  </span>

                  <h3>{event.name}</h3>

                  <p>{event.description}</p>

                  <div className="home-event-footer">
                    <span>{event.date}</span>
                    <span>{event.location}</span>
                  </div>
                </Link>
              ))}
            </div>
          )}
      </section>

      {/* CTA */}
      <section className="home-cta">
        <div className="home-container home-cta-inner">
          <div>
            <span className="home-eyebrow home-eyebrow-light">
              SIAP IKUT BERPARTISIPASI?
            </span>

            <h2>
              Kegiatan berikutnya
              <br />
              menunggumu.
            </h2>
          </div>

          <Link
            to="/events"
            className="home-cta-button"
          >
            <span>Jelajahi kegiatan</span>
            <strong>â†—</strong>
          </Link>
        </div>
      </section>

      {/* FOOTER */}
      <footer className="home-footer">
        <div className="home-container home-footer-inner">
          <div className="home-footer-brand">
            <span className="home-brand-mark">
              SE
            </span>

            <div>
              <strong>
                School Event Planner
              </strong>

              <span>
                Platform kegiatan sekolah
              </span>
            </div>
          </div>

          <span>
            Â© 2026 School Event Planner
          </span>
        </div>
      </footer>
    </main>
  );
}

export default Home;


===== FILE: E:\School Event Planner\frontend\src\pages\Login.jsx =====

import { useState } from "react";
import { Link, useNavigate } from "react-router-dom";
import "../styles/Login.css";

const API_URL =
  import.meta.env.VITE_API_URL ||
  "http://127.0.0.1:8000";

function Login() {
  const navigate = useNavigate();

  const [form, setForm] = useState({
    email: "",
    password: "",
  });

  const [loading, setLoading] = useState(false);
  const [error, setError] = useState("");

  function handleChange(event) {
    setForm({
      ...form,
      [event.target.name]: event.target.value,
    });
  }

  async function handleSubmit(event) {
  event.preventDefault();
  setError("");
  setLoading(true);

  try {
    const response = await fetch(`${API_URL}/auth/login`, {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify(form),
    });

    const data = await response.json();

    if (!response.ok) {
      throw new Error(
        data.detail || "Email atau password salah."
      );
    }

    if (!data.access_token) {
      throw new Error("Token login tidak diterima.");
    }

    localStorage.setItem(
      "access_token",
      data.access_token
    );

    if (data.user) {
      localStorage.setItem(
        "user",
        JSON.stringify(data.user)
      );
    } else {
      localStorage.removeItem("user");
    }

    const role = data.user?.role || data.role;

    if (role === "admin") {
      navigate("/admin", { replace: true });
    } else if (
      role === "committee" ||
      role === "staff"
    ) {
      navigate("/staff", { replace: true });
    } else {
      navigate("/dashboard", { replace: true });
    }
  } catch (err) {
    setError(
      err.message || "Gagal masuk ke akun."
    );
  } finally {
    setLoading(false);
  }
}

  return (
    <main className="login-page">
      <section className="login-brand-panel">
        <Link
          to="/"
          className="login-brand"
        >
          <span className="login-brand-mark">
            SE
          </span>

          <span>
            <strong>
              School Event
            </strong>

            <small>
              Planner
            </small>
          </span>
        </Link>

        <div className="login-brand-content">
          <span className="login-eyebrow">
            SCHOOL EVENT PLANNER
          </span>

          <h1>
            Semua kegiatan
            <br />
            <span>
              dalam satu tempat.
            </span>
          </h1>

          <p>
            Masuk untuk mengelola
            pendaftaran, mengikuti lomba,
            dan melihat aktivitas kegiatan
            sekolahmu.
          </p>
        </div>

        <div className="login-brand-footer">
          <span>EVENTS</span>
          <span>PARTICIPANTS</span>
          <span>2026</span>
        </div>
      </section>

      <section className="login-form-panel">
        <div className="login-form-box">
          <Link
            to="/"
            className="login-mobile-mark"
          >
            SE
          </Link>

          <div className="login-heading">
            <span className="login-eyebrow dark">
              WELCOME BACK
            </span>

            <h2>
              Masuk ke akun
            </h2>

            <p>
              Gunakan akunmu untuk
              melanjutkan.
            </p>
          </div>

          {error && (
            <div className="login-alert">
              <strong>
                Gagal masuk
              </strong>

              <span>
                {error}
              </span>
            </div>
          )}

          <form
            className="login-form"
            onSubmit={handleSubmit}
          >
            <div className="login-field">
              <label htmlFor="email">
                Email
              </label>

              <input
                id="email"
                name="email"
                type="email"
                placeholder="Masukkan email"
                value={form.email}
                onChange={handleChange}
                autoComplete="email"
                required
              />
            </div>

            <div className="login-field">
              <div className="login-label-row">
                <label htmlFor="password">
                  Password
                </label>
              </div>

              <input
                id="password"
                name="password"
                type="password"
                placeholder="Masukkan password"
                value={form.password}
                onChange={handleChange}
                autoComplete="current-password"
                required
              />
            </div>

            <button
              type="submit"
              className="login-submit"
              disabled={loading}
            >
              {loading ? (
                <>
                  <span className="login-button-spinner" />
                  Memproses...
                </>
              ) : (
                <>
                  Masuk
                  <span>â†—</span>
                </>
              )}
            </button>
          </form>

          <div className="login-register">
            <span>
              Belum punya akun?
            </span>

            <Link to="/register">
              Daftar sekarang
            </Link>
          </div>

          <Link
            to="/"
            className="login-back"
          >
            â† Kembali ke beranda
          </Link>
        </div>
      </section>
    </main>
  );
}

export default Login;


===== FILE: E:\School Event Planner\frontend\src\pages\Register.jsx =====

import { useState } from "react";
import { Link, useNavigate } from "react-router-dom";
import "../styles/Register.css";

const API_URL =
  import.meta.env.VITE_API_URL ||
  "http://127.0.0.1:8000";

function Register() {
  const navigate = useNavigate();

  const [form, setForm] = useState({
    name: "",
    email: "",
    password: "",
    confirmPassword: "",
  });

  const [error, setError] = useState("");
  const [loading, setLoading] = useState(false);

  function handleChange(e) {
    const { name, value } = e.target;

    setForm((prev) => ({
      ...prev,
      [name]: value,
    }));
  }

  async function handleSubmit(e) {
    e.preventDefault();
    setError("");

    if (form.password !== form.confirmPassword) {
      setError("Konfirmasi password tidak cocok.");
      return;
    }

    setLoading(true);

    try {
      const response = await fetch(`${API_URL}/auth/register`, {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          name: form.name.trim(),
          email: form.email.trim(),
          password: form.password,
        }),
      });

      const data = await response.json();

      if (!response.ok) {
        if (response.status === 422 && Array.isArray(data.detail)) {
          const messages = data.detail.map((item) => {
            const field = item.loc?.at(-1) || "field";
            return `${field}: ${item.msg}`;
          });

          throw new Error(messages.join(", "));
        }

        throw new Error(
          data.detail || "Registrasi gagal."
        );
      }

      navigate("/login");
    } catch (err) {
      setError(err.message || "Terjadi kesalahan.");
    } finally {
      setLoading(false);
    }
  }

  return (
    <main className="register-page">
      <section className="register-brand">
        <Link to="/" className="register-brand-logo">
          <span className="register-brand-mark">SE</span>

          <span>
            <strong>School Event</strong>
            <small>Planner</small>
          </span>
        </Link>

        <div className="register-brand-content">
          <span className="register-eyebrow">
            SCHOOL EVENT PLANNER
          </span>

          <h1>
            Mulai ikut
            <br />
            <span>kegiatan sekolah.</span>
          </h1>

          <p>
            Buat akun untuk mendaftar lomba, melihat
            kegiatan yang kamu ikuti, dan memantau
            status pendaftaranmu.
          </p>
        </div>

        <div className="register-brand-footer">
          <span>EVENTS Â· ACTIVITIES Â· PARTICIPATION</span>
          <span>2026</span>
        </div>
      </section>

      <section className="register-form-side">
        <div className="register-form-box">
          <Link to="/" className="register-mobile-logo">
            SE
          </Link>

          <span className="register-form-eyebrow">
            BUAT AKUN
          </span>

          <h2>Daftar</h2>

          <p className="register-subtitle">
            Isi data berikut untuk membuat akun peserta.
          </p>

          {error && (
            <div className="register-alert">
              <strong>!</strong>
              <span>{error}</span>
            </div>
          )}

          <form
            className="register-form"
            onSubmit={handleSubmit}
          >
            <div className="register-field">
              <label htmlFor="name">
                Nama lengkap
              </label>

              <input
                id="name"
                name="name"
                type="text"
                placeholder="Masukkan nama lengkap"
                value={form.name}
                onChange={handleChange}
                autoComplete="name"
                required
              />
            </div>

            <div className="register-field">
              <label htmlFor="email">
                Email
              </label>

              <input
                id="email"
                name="email"
                type="email"
                placeholder="Masukkan email"
                value={form.email}
                onChange={handleChange}
                autoComplete="email"
                required
              />
            </div>

            <div className="register-field">
              <label htmlFor="password">
                Password
              </label>

              <input
                id="password"
                name="password"
                type="password"
                placeholder="Buat password"
                value={form.password}
                onChange={handleChange}
                autoComplete="new-password"
                required
              />
            </div>

            <div className="register-field">
              <label htmlFor="confirmPassword">
                Konfirmasi password
              </label>

              <input
                id="confirmPassword"
                name="confirmPassword"
                type="password"
                placeholder="Ulangi password"
                value={form.confirmPassword}
                onChange={handleChange}
                autoComplete="new-password"
                required
              />
            </div>

            <button
              type="submit"
              className="register-submit"
              disabled={loading}
            >
              {loading
                ? "Membuat akun..."
                : "Buat akun"}
            </button>
          </form>

          <div className="register-switch">
            <span>Sudah punya akun?</span>
            <Link to="/login">Masuk</Link>
          </div>

          <Link to="/" className="register-back">
            â† Kembali ke beranda
          </Link>
        </div>
      </section>
    </main>
  );
}

export default Register;


===== FILE: E:\School Event Planner\frontend\src\pages\StaffDashboard.jsx =====

import { useEffect, useState } from "react";
import { Link, useNavigate } from "react-router-dom";
import { getEvents } from "../services/api";
import "../styles/StaffDashboard.css";

const API_URL =
  import.meta.env.VITE_API_URL || "http://127.0.0.1:8000";

const emptyForm = {
  name: "",
  description: "",
  category: "individual",
  date: "",
  start_time: "",
  end_time: "",
  location: "",
  max_participants: 50,
};

function StaffDashboard() {
  const navigate = useNavigate();

  const [events, setEvents] = useState([]);
  const [loading, setLoading] = useState(true);
  const [saving, setSaving] = useState(false);
  const [error, setError] = useState("");
  const [showForm, setShowForm] = useState(false);
  const [editingId, setEditingId] = useState(null);
  const [form, setForm] = useState(emptyForm);

  const token = localStorage.getItem("access_token");

  useEffect(() => {
    if (!token) {
      navigate("/login", { replace: true });
      return;
    }

    loadEvents();
  }, [navigate, token]);

  async function loadEvents() {
    try {
      setLoading(true);
      setError("");

      const data = await getEvents();

      setEvents(Array.isArray(data) ? data : []);
    } catch (err) {
      setError(
        err.message || "Gagal memuat kegiatan."
      );
    } finally {
      setLoading(false);
    }
  }

  function handleChange(event) {
    const { name, value } = event.target;

    setForm((current) => ({
      ...current,
      [name]:
        name === "max_participants"
          ? Number(value)
          : value,
    }));
  }

  function openCreateForm() {
    setEditingId(null);
    setForm({ ...emptyForm });
    setError("");
    setShowForm(true);
  }

  function openEditForm(item) {
    setEditingId(item.id);

    setForm({
      name: item.name || "",
      description: item.description || "",
      category: item.category || "individual",
      date: item.date || "",
      start_time: item.start_time || "",
      end_time: item.end_time || "",
      location: item.location || "",
      max_participants:
        item.max_participants || 50,
    });

    setError("");
    setShowForm(true);
  }

  function closeForm() {
    setShowForm(false);
    setEditingId(null);
    setForm({ ...emptyForm });
  }

  async function handleSubmit(event) {
    event.preventDefault();

    setSaving(true);
    setError("");

    try {
      const url = editingId
        ? `${API_URL}/events/${editingId}`
        : `${API_URL}/events`;

      const response = await fetch(url, {
        method: editingId ? "PUT" : "POST",
        headers: {
          "Content-Type": "application/json",
          Authorization: `Bearer ${token}`,
        },
        body: JSON.stringify(form),
      });

      const data = await response.json();

      if (!response.ok) {
        throw new Error(
          data.detail ||
            "Gagal menyimpan kegiatan."
        );
      }

      closeForm();
      await loadEvents();
    } catch (err) {
      setError(
        err.message ||
          "Gagal menyimpan kegiatan."
      );
    } finally {
      setSaving(false);
    }
  }

  async function handleDelete(id) {
    if (
      !window.confirm(
        "Yakin ingin menghapus kegiatan ini?"
      )
    ) {
      return;
    }

    try {
      setError("");

      const response = await fetch(
        `${API_URL}/events/${id}`,
        {
          method: "DELETE",
          headers: {
            Authorization: `Bearer ${token}`,
          },
        }
      );

      const data = await response.json();

      if (!response.ok) {
        throw new Error(
          data.detail ||
            "Gagal menghapus kegiatan."
        );
      }

      await loadEvents();
    } catch (err) {
      setError(
        err.message ||
          "Gagal menghapus kegiatan."
      );
    }
  }

  function handleLogout() {
    localStorage.removeItem("access_token");
    localStorage.removeItem("user");

    navigate("/login", {
      replace: true,
    });
  }

  const openCount = events.filter(
    (item) => item.registration_open
  ).length;

  const teamCount = events.filter(
    (item) => item.category === "team"
  ).length;

  const individualCount = events.filter(
    (item) => item.category === "individual"
  ).length;

  return (
    <main className="staff-page">
      <header className="staff-header">
        <div className="staff-container staff-header-inner">
          <Link
            to="/"
            className="staff-brand"
          >
            <span className="staff-brand-mark">
              SE
            </span>

            <span className="staff-brand-name">
              School Event
              <strong>Planner</strong>
            </span>
          </Link>

          <nav className="staff-nav">
            <Link to="/events">
              Kegiatan
            </Link>

            

            <span className="staff-role">
              PANITIA
            </span>

            <button
              type="button"
              className="staff-logout"
              onClick={handleLogout}
            >
              Keluar
            </button>
          </nav>
        </div>
      </header>

      <section className="staff-container staff-hero">
        <div className="staff-hero-content">
          <span className="staff-eyebrow">
            PANEL PANITIA
          </span>

          <h1>
            Kelola kegiatan.
          </h1>

          <p>
            Buat, edit, dan kelola seluruh
            kegiatan sekolah dari satu tempat.
          </p>
        </div>

        <button
          type="button"
          className="staff-primary-button"
          onClick={openCreateForm}
        >
          <span>+</span>
          Buat kegiatan
        </button>
      </section>

      <section className="staff-container staff-stats">
        <article className="staff-stat staff-stat-primary">
          <span>
            TOTAL KEGIATAN
          </span>

          <strong>
            {events.length}
          </strong>

          <small>
            Semua kegiatan
          </small>
        </article>

        <article className="staff-stat">
          <span>
            TERBUKA
          </span>

          <strong>
            {openCount}
          </strong>

          <small>
            Pendaftaran aktif
          </small>
        </article>

        <article className="staff-stat">
          <span>
            TIM
          </span>

          <strong>
            {teamCount}
          </strong>

          <small>
            Kegiatan berkelompok
          </small>
        </article>

        <article className="staff-stat">
          <span>
            INDIVIDU
          </span>

          <strong>
            {individualCount}
          </strong>

          <small>
            Kegiatan individu
          </small>
        </article>
      </section>

      <section className="staff-container staff-content">
        {error && (
          <div className="staff-alert">
            <strong>!</strong>
            <span>{error}</span>
          </div>
        )}

        <div className="staff-section-heading">
          <div>
            <span className="staff-eyebrow">
              DAFTAR KEGIATAN
            </span>

            <h2>
              Semua kegiatan
            </h2>
          </div>

          <span className="staff-result-count">
            {events.length} kegiatan
          </span>
        </div>

        {loading ? (
          <div className="staff-state">
            <div className="staff-spinner" />

            <strong>
              Memuat kegiatan
            </strong>

            <span>
              Menyiapkan panel panitia...
            </span>
          </div>
        ) : events.length === 0 ? (
          <div className="staff-empty">
            <div className="staff-empty-icon">
              +
            </div>

            <span className="staff-eyebrow">
              BELUM ADA DATA
            </span>

            <h3>
              Belum ada kegiatan.
            </h3>

            <p>
              Buat kegiatan pertama untuk
              mulai mengisi event planner.
            </p>

            <button
              type="button"
              className="staff-primary-button"
              onClick={openCreateForm}
            >
              + Buat kegiatan
            </button>
          </div>
        ) : (
          <div className="staff-event-list">
            {events.map((item, index) => (
              <article
                className="staff-event-card"
                key={item.id}
              >
                <div className="staff-event-number">
                  {String(index + 1).padStart(
                    2,
                    "0"
                  )}
                </div>

                <div className="staff-event-info">
                  <div className="staff-tag-row">
                    <span className="staff-tag">
                      {item.category === "team"
                        ? "TIM"
                        : "INDIVIDU"}
                    </span>

                    <span
                      className={
                        item.registration_open
                          ? "staff-status open"
                          : "staff-status closed"
                      }
                    >
                      {item.registration_open
                        ? "Pendaftaran dibuka"
                        : "Ditutup"}
                    </span>
                  </div>

                  <h3>
                    {item.name}
                  </h3>

                  <p>
                    {item.date}
                    {" Â· "}
                    {item.start_time}
                    {" â€“ "}
                    {item.end_time}
                    {" Â· "}
                    {item.location}
                  </p>
                </div>

                <div className="staff-event-limit">
                  <span>
                    MAKS. PESERTA
                  </span>

                  <strong>
                    {item.max_participants}
                  </strong>
                </div>

                <div className="staff-event-actions">
                  <button
                    type="button"
                    onClick={() =>
                      openEditForm(item)
                    }
                  >
                    Edit
                  </button>

                  <button
                    type="button"
                    className="danger"
                    onClick={() =>
                      handleDelete(item.id)
                    }
                  >
                    Hapus
                  </button>
                </div>
              </article>
            ))}
          </div>
        )}
      </section>

      {showForm && (
        <div
          className="staff-modal-backdrop"
          onMouseDown={closeForm}
        >
          <section
            className="staff-modal"
            onMouseDown={(event) =>
              event.stopPropagation()
            }
          >
            <div className="staff-modal-header">
              <div>
                <span className="staff-eyebrow">
                  {editingId
                    ? "EDIT KEGIATAN"
                    : "KEGIATAN BARU"}
                </span>

                <h2>
                  {editingId
                    ? "Edit kegiatan"
                    : "Buat kegiatan"}
                </h2>
              </div>

              <button
                type="button"
                className="staff-modal-close"
                onClick={closeForm}
              >
                Ã—
              </button>
            </div>

            <form
              className="staff-form"
              onSubmit={handleSubmit}
            >
              <div className="staff-field">
                <label htmlFor="name">
                  Nama kegiatan
                </label>

                <input
                  id="name"
                  name="name"
                  value={form.name}
                  onChange={handleChange}
                  placeholder="Contoh: Futsal Putra"
                  required
                  minLength={2}
                />
              </div>

              <div className="staff-field">
                <label htmlFor="description">
                  Deskripsi
                </label>

                <textarea
                  id="description"
                  name="description"
                  value={form.description}
                  onChange={handleChange}
                  placeholder="Jelaskan kegiatan..."
                  required
                  rows={4}
                />
              </div>

              <div className="staff-field-grid">
                <div className="staff-field">
                  <label htmlFor="category">
                    Kategori
                  </label>

                  <select
                    id="category"
                    name="category"
                    value={form.category}
                    onChange={handleChange}
                  >
                    <option value="individual">
                      Individu
                    </option>

                    <option value="team">
                      Tim
                    </option>
                  </select>
                </div>

                <div className="staff-field">
                  <label htmlFor="max_participants">
                    Maks. peserta
                  </label>

                  <input
                    id="max_participants"
                    name="max_participants"
                    type="number"
                    min="1"
                    max="1000"
                    value={
                      form.max_participants
                    }
                    onChange={handleChange}
                    required
                  />
                </div>
              </div>

              <div className="staff-field-grid">
                <div className="staff-field">
                  <label htmlFor="date">
                    Tanggal
                  </label>

                  <input
                    id="date"
                    name="date"
                    type="date"
                    value={form.date}
                    onChange={handleChange}
                    required
                  />
                </div>

                <div className="staff-field">
                  <label htmlFor="location">
                    Lokasi
                  </label>

                  <input
                    id="location"
                    name="location"
                    value={form.location}
                    onChange={handleChange}
                    placeholder="Lokasi kegiatan"
                    required
                  />
                </div>
              </div>

              <div className="staff-field-grid">
                <div className="staff-field">
                  <label htmlFor="start_time">
                    Waktu mulai
                  </label>

                  <input
                    id="start_time"
                    name="start_time"
                    type="time"
                    value={form.start_time}
                    onChange={handleChange}
                    required
                  />
                </div>

                <div className="staff-field">
                  <label htmlFor="end_time">
                    Waktu selesai
                  </label>

                  <input
                    id="end_time"
                    name="end_time"
                    type="time"
                    value={form.end_time}
                    onChange={handleChange}
                    required
                  />
                </div>
              </div>

              <div className="staff-modal-actions">
                <button
                  type="button"
                  className="staff-secondary-button"
                  onClick={closeForm}
                >
                  Batal
                </button>

                <button
                  type="submit"
                  className="staff-primary-button"
                  disabled={saving}
                >
                  {saving
                    ? "Menyimpan..."
                    : editingId
                      ? "Simpan perubahan"
                      : "Buat kegiatan"}
                </button>
              </div>
            </form>
          </section>
        </div>
      )}
    </main>
  );
}

export default StaffDashboard;


===== FILE: E:\School Event Planner\frontend\src\services\api.js =====

export const API_URL =
  import.meta.env.VITE_API_URL ||
  "http://127.0.0.1:8000";

console.log("API_URL:", API_URL);

async function request(endpoint, options = {}) {
  const response = await fetch(
    `${API_URL}${endpoint}`,
    {
      headers: {
        "Content-Type": "application/json",
        ...options.headers,
      },
      ...options,
    }
  );

  const data = await response.json();

  if (!response.ok) {
    throw new Error(
      data.detail || "Terjadi kesalahan."
    );
  }

  return data;
}


export async function getEvents() {
  return request("/events");
}


export async function getEvent(id) {
  return request(`/events/${id}`);
}


export async function getAnnouncements() {
  return request("/announcements");
}


export async function getResults() {
  return request("/results");
}


export async function registerEvent(eventId) {
  const token = localStorage.getItem("access_token");

  if (!token) {
    throw new Error("LOGIN_REQUIRED");
  }

  return request(
    `/registrations/${eventId}`,
    {
      method: "POST",
      headers: {
        Authorization: `Bearer ${token}`,
      },
    }
  );
}


===== FILE: E:\School Event Planner\frontend\src\styles\Announcements.css =====

.announcements-page {
  min-height: 100vh;
  background: #fafafa;
  color: #15171b;
}

.announcements-container {
  width: min(1180px, calc(100% - 40px));
  margin: 0 auto;
}

/* HEADER */

.announcements-header {
  position: relative;
  z-index: 20;
  min-height: 76px;
  background: rgba(255, 255, 255, 0.94);
  border-bottom: 1px solid #e9e9ec;
  backdrop-filter: blur(18px);
}

.announcements-header-inner {
  min-height: 76px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 25px;
}

.announcements-brand {
  display: inline-flex;
  align-items: center;
  gap: 11px;
  font-family: "Manrope", sans-serif;
  font-weight: 700;
}

.announcements-brand-mark {
  width: 40px;
  height: 40px;
  display: grid;
  place-items: center;
  flex-shrink: 0;
  border-radius: 12px;
  color: white;
  background: #c31320;
  font-size: 13px;
  box-shadow: 0 9px 22px rgba(195, 19, 32, 0.22);
}

.announcements-brand-name {
  color: #15171b;
  line-height: 1.05;
}

.announcements-brand-name strong {
  display: block;
  color: #c31320;
}

.announcements-nav {
  display: flex;
  align-items: center;
  gap: 7px;
}

.announcements-nav-link {
  padding: 10px 13px;
  border-radius: 10px;
  color: #696c73;
  font-size: 13px;
  font-weight: 700;
  transition: 0.2s ease;
}

.announcements-nav-link:hover,
.announcements-nav-link.active {
  color: #c31320;
  background: #fff5f6;
}

.announcements-nav-button {
  padding: 11px 17px;
  border-radius: 11px;
  color: white;
  background: #c31320;
  font-size: 13px;
  font-weight: 800;
}

/* HERO */

.announcements-hero {
  position: relative;
  overflow: hidden;
  padding: 65px 0 70px;
  color: white;
  background:
    radial-gradient(
      circle at 82% 20%,
      rgba(255, 255, 255, 0.14),
      transparent 27%
    ),
    radial-gradient(
      circle at 8% 100%,
      rgba(255, 255, 255, 0.08),
      transparent 30%
    ),
    linear-gradient(
      135deg,
      #920d17,
      #c31320 55%,
      #850b14
    );
}

.announcements-hero::after {
  content: "";
  position: absolute;
  width: 380px;
  height: 380px;
  right: -190px;
  bottom: -270px;
  border: 1px solid rgba(255, 255, 255, 0.12);
  border-radius: 50%;
}

.announcements-eyebrow {
  position: relative;
  z-index: 1;
  display: inline-flex;
  color: #ffd8dc;
  font-size: 11px;
  font-weight: 800;
  letter-spacing: 0.16em;
}

.announcements-hero h1 {
  position: relative;
  z-index: 1;
  max-width: 720px;
  margin: 18px 0 17px;
  font-family: "Manrope", sans-serif;
  font-size: clamp(40px, 5.5vw, 62px);
  line-height: 1;
  letter-spacing: -0.06em;
}

.announcements-hero h1 span {
  color: #ffd9dd;
}

.announcements-hero p {
  position: relative;
  z-index: 1;
  max-width: 620px;
  margin: 0;
  color: rgba(255, 255, 255, 0.75);
  font-size: 15px;
  line-height: 1.75;
}

/* CONTENT */

.announcements-content {
  padding: 70px 0 110px;
}

/* STATES */

.announcements-state {
  min-height: 300px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  gap: 8px;
  padding: 35px;
  border: 1px solid #e9e9ec;
  border-radius: 22px;
  background: white;
  color: #74777e;
  text-align: center;
}

.announcements-state strong {
  color: #15171b;
  font-family: "Manrope", sans-serif;
}

.announcements-state p {
  max-width: 480px;
  margin: 3px 0 0;
  color: #74777e;
  font-size: 13px;
}

.announcements-spinner {
  width: 34px;
  height: 34px;
  margin-bottom: 7px;
  border: 3px solid #f1d5d8;
  border-top-color: #c31320;
  border-radius: 50%;
  animation: announcements-spin 0.8s linear infinite;
}

@keyframes announcements-spin {
  to {
    transform: rotate(360deg);
  }
}

.announcements-state.error {
  border-color: #f0d0d3;
}

.announcements-state-label {
  color: #c31320;
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 0.15em;
}

.announcements-state.empty {
  min-height: 330px;
}

.announcements-empty-icon {
  width: 52px;
  height: 52px;
  display: grid;
  place-items: center;
  margin-bottom: 8px;
  border-radius: 16px;
  color: #c31320;
  background: #fff0f1;
  font-size: 24px;
}

/* LIST */

.announcements-list {
  display: grid;
  gap: 14px;
}

.announcement-card {
  display: grid;
  grid-template-columns: 55px minmax(0, 1fr);
  gap: 24px;
  padding: 26px 28px;
  border: 1px solid #e8e8eb;
  border-radius: 20px;
  background: white;
  transition:
    transform 0.2s ease,
    border-color 0.2s ease,
    box-shadow 0.2s ease;
}

.announcement-card:hover {
  border-color: #e7b9bd;
  box-shadow: 0 18px 45px rgba(21, 23, 27, 0.06);
  transform: translateY(-2px);
}

.announcement-number {
  color: #c8cbd1;
  font-family: "Manrope", sans-serif;
  font-size: 22px;
  font-weight: 800;
}

.announcement-main {
  min-width: 0;
}

.announcement-date {
  display: inline-flex;
  padding: 5px 9px;
  border-radius: 7px;
  color: #c31320;
  background: #fff0f1;
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 0.06em;
}

.announcement-main h2 {
  margin: 12px 0 8px;
  font-family: "Manrope", sans-serif;
  font-size: 22px;
  letter-spacing: -0.03em;
}

.announcement-main p {
  margin: 0;
  color: #6e727a;
  font-size: 13px;
  line-height: 1.75;
  white-space: pre-line;
}

/* FOOTER */

.announcements-footer {
  padding: 26px 0;
  color: #858990;
  background: #121316;
}

.announcements-footer-inner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 25px;
  font-size: 11px;
}

.announcements-footer-inner strong,
.announcements-footer-inner span {
  display: block;
}

.announcements-footer-inner strong {
  margin-bottom: 4px;
  color: white;
  font-size: 12px;
}

/* RESPONSIVE */

@media (max-width: 720px) {
  .announcements-container {
    width: min(100% - 28px, 1180px);
  }

  .announcements-header,
  .announcements-header-inner {
    min-height: 68px;
  }

  .announcements-brand-mark {
    width: 36px;
    height: 36px;
  }

  .announcements-nav-link {
    display: none;
  }

  .announcements-nav-button {
    padding: 9px 13px;
    font-size: 12px;
  }

  .announcements-hero {
    padding: 55px 0 55px;
  }

  .announcements-hero h1 {
    font-size: 40px;
  }

  .announcements-content {
    padding: 55px 0 75px;
  }

  .announcement-card {
    grid-template-columns: 1fr;
    gap: 10px;
    padding: 22px;
  }

  .announcement-number {
    display: none;
  }

  .announcements-footer-inner {
    align-items: flex-start;
    flex-direction: column;
  }
}

@media (max-width: 460px) {
  .announcements-hero h1 {
    font-size: 34px;
  }
}


===== FILE: E:\School Event Planner\frontend\src\styles\Dashboard.css =====

.dashboard-page {
  min-height: 100vh;
  background: #f8f8f9;
  color: #15171b;
}

.dashboard-header {
  position: sticky;
  top: 0;
  z-index: 20;
  border-bottom: 1px solid #e9e9ec;
  background: rgba(255, 255, 255, 0.94);
  backdrop-filter: blur(18px);
}

.dashboard-header-inner {
  width: min(1180px, calc(100% - 40px));
  min-height: 76px;
  margin: 0 auto;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 25px;
}

.dashboard-brand {
  display: inline-flex;
  align-items: center;
  gap: 11px;
  font-family: "Manrope", sans-serif;
}

.dashboard-brand-mark {
  width: 40px;
  height: 40px;
  display: grid;
  place-items: center;
  border-radius: 12px;
  color: white;
  background: #c31320;
  box-shadow: 0 9px 22px rgba(195, 19, 32, 0.2);
  font-size: 13px;
  font-weight: 800;
}

.dashboard-brand strong,
.dashboard-brand small {
  display: block;
}

.dashboard-brand strong {
  font-size: 14px;
}

.dashboard-brand small {
  margin-top: 2px;
  color: #8a8d94;
  font-size: 10px;
  font-weight: 600;
}

.dashboard-nav {
  display: flex;
  align-items: center;
  gap: 8px;
}

.dashboard-nav > a {
  padding: 10px 14px;
  border-radius: 10px;
  color: #5f636b;
  font-size: 13px;
  font-weight: 700;
}

.dashboard-nav > a:hover {
  color: #c31320;
  background: #fff7f7;
}

.dashboard-logout {
  padding: 10px 15px;
  border: 1px solid #e9e9ec;
  border-radius: 11px;
  color: #15171b;
  background: white;
  font-size: 12px;
  font-weight: 800;
}

.dashboard-logout:hover {
  color: #c31320;
  border-color: #efc4c7;
}

.dashboard-shell {
  width: min(1180px, calc(100% - 40px));
  margin: 0 auto;
  padding: 65px 0 100px;
}

.dashboard-hero {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: 30px;
  margin-bottom: 42px;
}

.dashboard-eyebrow {
  display: inline-flex;
  color: #c31320;
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 0.16em;
}

.dashboard-hero h1 {
  margin: 10px 0;
  font-family: "Manrope", sans-serif;
  font-size: clamp(38px, 5vw, 58px);
  line-height: 1;
  letter-spacing: -0.06em;
}

.dashboard-hero h1 span {
  color: #c31320;
}

.dashboard-hero p {
  margin: 0;
  color: #6e727a;
  font-size: 14px;
}

.dashboard-profile {
  min-width: 205px;
  display: flex;
  align-items: center;
  gap: 11px;
  padding: 7px 14px 7px 7px;
  border: 1px solid #e9e9ec;
  border-radius: 40px;
  background: white;
}

.dashboard-avatar {
  width: 38px;
  height: 38px;
  display: grid;
  place-items: center;
  border-radius: 50%;
  color: white;
  background: #c31320;
  font-size: 13px;
  font-weight: 800;
}

.dashboard-profile strong,
.dashboard-profile span {
  display: block;
}

.dashboard-profile strong {
  font-size: 12px;
}

.dashboard-profile span {
  margin-top: 2px;
  color: #999ca3;
  font-size: 10px;
}

.dashboard-alert {
  display: flex;
  align-items: flex-start;
  gap: 10px;
  margin-bottom: 18px;
  padding: 13px 15px;
  border: 1px solid #ffd4d7;
  border-radius: 12px;
  color: #8e151d;
  background: #fff0f1;
  font-size: 12px;
  line-height: 1.5;
}

.dashboard-alert strong {
  width: 19px;
  height: 19px;
  display: grid;
  flex: 0 0 auto;
  place-items: center;
  border-radius: 50%;
  color: white;
  background: #c31320;
  font-size: 11px;
}

.dashboard-stats {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 13px;
}

.dashboard-stat {
  min-height: 145px;
  padding: 22px;
  border: 1px solid #e9e9ec;
  border-radius: 18px;
  background: white;
}

.dashboard-stat-accent {
  color: white;
  border-color: #c31320;
  background: #c31320;
}

.dashboard-stat > span {
  display: block;
  margin-bottom: 16px;
  color: #8b8e95;
  font-size: 9px;
  font-weight: 800;
  letter-spacing: 0.12em;
}

.dashboard-stat-accent > span {
  color: rgba(255, 255, 255, 0.68);
}

.dashboard-stat strong {
  display: block;
  margin-bottom: 5px;
  font-family: "Manrope", sans-serif;
  font-size: 34px;
  letter-spacing: -0.05em;
}

.dashboard-stat small {
  color: #989ba1;
  font-size: 10px;
}

.dashboard-stat-accent small {
  color: rgba(255, 255, 255, 0.62);
}

.dashboard-active {
  color: #16834b;
  font-size: 22px !important;
  padding-top: 6px;
}

.dashboard-section {
  margin-top: 85px;
}

.dashboard-section-heading {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: 25px;
  margin-bottom: 28px;
}

.dashboard-section-heading h2 {
  margin: 8px 0 0;
  font-family: "Manrope", sans-serif;
  font-size: 32px;
  letter-spacing: -0.045em;
}

.dashboard-section-heading > a {
  color: #c31320;
  font-size: 12px;
  font-weight: 800;
}

.dashboard-section-heading > a:hover {
  text-decoration: underline;
}

.registration-list {
  display: grid;
  gap: 12px;
}

.registration-card {
  min-height: 105px;
  display: grid;
  grid-template-columns: 55px minmax(0, 1fr) auto auto;
  align-items: center;
  gap: 20px;
  padding: 17px 20px;
  border: 1px solid #e9e9ec;
  border-radius: 18px;
  background: white;
  transition: 0.2s ease;
}

.registration-card:hover {
  border-color: #efc4c7;
  box-shadow: 0 15px 35px rgba(20, 22, 26, 0.05);
  transform: translateY(-2px);
}

.registration-number {
  color: #b7bac0;
  font-size: 11px;
  font-weight: 800;
}

.registration-main {
  min-width: 0;
}

.registration-category {
  display: inline-flex;
  padding: 5px 8px;
  border-radius: 7px;
  color: #c31320;
  background: #fff0f1;
  font-size: 9px;
  font-weight: 800;
  letter-spacing: 0.08em;
}

.registration-main h3 {
  margin: 7px 0 4px;
  overflow: hidden;
  font-family: "Manrope", sans-serif;
  font-size: 18px;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.registration-main p {
  margin: 0;
  color: #6e727a;
  font-size: 11px;
}

.registration-status {
  display: inline-flex;
  width: fit-content;
  padding: 7px 9px;
  border-radius: 8px;
  font-size: 9px;
  font-weight: 800;
}

.registration-status.approved {
  color: #137242;
  background: #ebf8f0;
}

.registration-status.pending {
  color: #8d6712;
  background: #fff6dc;
}

.registration-status.closed {
  color: #70737a;
  background: #f0f0f2;
}

.registration-link {
  padding: 9px 12px;
  color: #c31320;
  font-size: 11px;
  font-weight: 800;
}

.registration-link:hover {
  text-decoration: underline;
}

.dashboard-empty {
  min-height: 330px;
  display: flex;
  align-items: center;
  flex-direction: column;
  justify-content: center;
  padding: 40px;
  text-align: center;
  border: 1px solid #e9e9ec;
  border-radius: 22px;
  background: white;
}

.dashboard-empty-icon {
  width: 52px;
  height: 52px;
  display: grid;
  place-items: center;
  margin-bottom: 17px;
  border-radius: 16px;
  color: #c31320;
  background: #fff0f1;
  font-size: 25px;
}

.dashboard-empty h3 {
  margin: 9px 0 7px;
  font-family: "Manrope", sans-serif;
  font-size: 22px;
}

.dashboard-empty p {
  max-width: 450px;
  margin: 0 0 22px;
  color: #6e727a;
  font-size: 13px;
  line-height: 1.7;
}

.dashboard-primary-button {
  min-height: 45px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 0 18px;
  border-radius: 12px;
  color: white;
  background: #c31320;
  font-size: 12px;
  font-weight: 800;
  box-shadow: 0 10px 25px rgba(195, 19, 32, 0.18);
}

.dashboard-primary-button:hover {
  background: #a70f1b;
  transform: translateY(-1px);
}

.dashboard-state {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  gap: 8px;
  color: #6e727a;
}

.dashboard-state strong {
  color: #15171b;
  font-family: "Manrope", sans-serif;
}

.dashboard-spinner {
  width: 34px;
  height: 34px;
  margin-bottom: 5px;
  border: 3px solid #f0d6d8;
  border-top-color: #c31320;
  border-radius: 50%;
  animation: dashboard-spin 0.8s linear infinite;
}

@keyframes dashboard-spin {
  to {
    transform: rotate(360deg);
  }
}

.dashboard-footer {
  min-height: 90px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 25px;
  padding: 25px max(20px, calc((100vw - 1180px) / 2));
  color: #858990;
  background: #121316;
  font-size: 11px;
}

.dashboard-footer strong,
.dashboard-footer span {
  display: block;
}

.dashboard-footer strong {
  margin-bottom: 4px;
  color: white;
  font-family: "Manrope", sans-serif;
}

.dashboard-footer div span {
  color: #777a81;
}

/* RESPONSIVE */

@media (max-width: 900px) {
  .dashboard-stats {
    grid-template-columns: repeat(2, 1fr);
  }

  .dashboard-profile {
    display: none;
  }

  .registration-card {
    grid-template-columns: 45px minmax(0, 1fr) auto;
  }

  .registration-link {
    grid-column: 2;
    padding: 0;
  }
}

@media (max-width: 720px) {
  .dashboard-header-inner,
  .dashboard-shell {
    width: min(100% - 28px, 1180px);
  }

  .dashboard-header-inner {
    min-height: 68px;
  }

  .dashboard-brand-mark {
    width: 36px;
    height: 36px;
  }

  .dashboard-brand strong {
    font-size: 13px;
  }

  .dashboard-nav > a {
    display: none;
  }

  .dashboard-logout {
    padding: 9px 12px;
  }

  .dashboard-shell {
    padding-top: 45px;
  }

  .dashboard-hero,
  .dashboard-section-heading {
    align-items: flex-start;
    flex-direction: column;
  }

  .dashboard-hero h1 {
    font-size: 42px;
  }

  .dashboard-stats {
    grid-template-columns: 1fr 1fr;
  }

  .dashboard-section {
    margin-top: 65px;
  }

  .registration-card {
    grid-template-columns: 38px minmax(0, 1fr);
    gap: 12px;
  }

  .registration-status {
    grid-column: 2;
    width: fit-content;
  }

  .registration-link {
    grid-column: 2;
  }

  .dashboard-footer {
    align-items: flex-start;
    flex-direction: column;
    padding: 25px 14px;
  }
}

@media (max-width: 460px) {
  .dashboard-stats {
    grid-template-columns: 1fr;
  }

  .dashboard-hero h1 {
    font-size: 38px;
  }

  .dashboard-empty {
    padding: 30px 20px;
  }
}


===== FILE: E:\School Event Planner\frontend\src\styles\EventDetail.css =====

.event-detail-page {
  min-height: 100vh;
  background: #f7f7f4;
  color: #151515;
}

.detail-container {
  width: min(1180px, calc(100% - 48px));
  margin: 0 auto;
}

/* HEADER */

.detail-header {
  height: 76px;
  background: #ffffff;
  border-bottom: 1px solid #e8e8e3;
}

.detail-header-inner {
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.detail-brand {
  display: inline-flex;
  align-items: center;
  gap: 12px;
  color: #151515;
  text-decoration: none;
}

.detail-brand-mark {
  width: 42px;
  height: 42px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #c31320;
  color: #ffffff;
  border-radius: 12px;
  font-size: 14px;
  font-weight: 800;
  letter-spacing: -0.04em;
}

.detail-brand-name {
  display: flex;
  flex-direction: column;
  font-size: 15px;
  line-height: 1.05;
  font-weight: 500;
}

.detail-brand-name strong {
  font-weight: 800;
}

.detail-nav {
  display: flex;
  align-items: center;
  gap: 28px;
}

.detail-nav-link {
  color: #666660;
  text-decoration: none;
  font-size: 14px;
  font-weight: 600;
  transition: color 0.2s ease;
}

.detail-nav-link:hover,
.detail-nav-link.active {
  color: #111111;
}

.detail-nav-button {
  min-height: 42px;
  padding: 0 20px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  border-radius: 10px;
  background: #d92828;
  color: #ffffff;
  text-decoration: none;
  font-size: 14px;
  font-weight: 700;
  transition: 0.2s ease;
}

.detail-nav-button:hover {
  background: #b91f1f;
  transform: translateY(-1px);
}

.detail-back {
  display: inline-flex;
  align-items: center;
  color: #686862;
  text-decoration: none;
  font-size: 14px;
  font-weight: 600;
  transition: color 0.2s ease;
}

.detail-back:hover {
  color: #111111;
}

/* HERO */

.detail-hero {
  background: #111111;
  color: #ffffff;
  padding: 64px 0 76px;
}

.detail-hero .detail-back {
  color: #a9a9a3;
  margin-bottom: 52px;
}

.detail-hero .detail-back:hover {
  color: #ffffff;
}

.detail-hero-content {
  max-width: 850px;
}

.detail-tag-row {
  display: flex;
  align-items: center;
  gap: 10px;
  margin-bottom: 22px;
}

.detail-tag,
.detail-status {
  display: inline-flex;
  align-items: center;
  min-height: 30px;
  padding: 0 11px;
  border: 1px solid rgba(255, 255, 255, 0.2);
  border-radius: 999px;
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 0.12em;
}

.detail-tag {
  background: #ffffff;
  color: #111111;
  border-color: #ffffff;
}

.detail-status {
  color: #bdbdb7;
}

.detail-hero h1 {
  max-width: 900px;
  margin: 0;
  font-size: clamp(44px, 6vw, 76px);
  line-height: 0.98;
  letter-spacing: -0.055em;
  font-weight: 800;
}

.detail-hero p {
  max-width: 700px;
  margin: 28px 0 0;
  color: #bdbdb7;
  font-size: 17px;
  line-height: 1.7;
}

/* CONTENT */

.detail-content {
  padding: 90px 0;
}

.detail-layout {
  display: grid;
  grid-template-columns: minmax(0, 1fr) 390px;
  gap: 80px;
  align-items: start;
}

.detail-main {
  min-width: 0;
}

.detail-eyebrow,
.detail-card-label,
.detail-info-card span {
  display: block;
  color: #8a8a83;
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 0.14em;
}

.detail-main h2 {
  margin: 12px 0 20px;
  font-size: 38px;
  line-height: 1.05;
  letter-spacing: -0.04em;
}

.detail-description {
  max-width: 760px;
  margin: 0;
  color: #60605a;
  font-size: 16px;
  line-height: 1.8;
}

.detail-info-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 12px;
  margin-top: 42px;
}

.detail-info-card {
  min-height: 105px;
  padding: 22px;
  background: #ffffff;
  border: 1px solid #e5e5df;
  border-radius: 16px;
}

.detail-info-card strong {
  display: block;
  margin-top: 12px;
  color: #171717;
  font-size: 15px;
  line-height: 1.45;
}

/* REGISTRATION */

.detail-registration-card {
  position: sticky;
  top: 24px;
  padding: 30px;
  background: #ffffff;
  border: 1px solid #e4e4de;
  border-radius: 22px;
  box-shadow: 0 18px 50px rgba(0, 0, 0, 0.06);
}

.detail-availability {
  display: flex;
  align-items: center;
  gap: 9px;
  margin-bottom: 32px;
  color: #686862;
  font-size: 12px;
  font-weight: 700;
}

.detail-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: #a3a39d;
}

.detail-dot.open {
  background: #29915b;
  box-shadow: 0 0 0 4px rgba(41, 145, 91, 0.1);
}

.detail-card-label {
  margin-bottom: 12px;
}

.detail-registration-card h2 {
  margin: 0;
  font-size: 32px;
  line-height: 1.05;
  letter-spacing: -0.045em;
}

.detail-registration-card > p {
  margin: 18px 0 24px;
  color: #70706a;
  font-size: 14px;
  line-height: 1.7;
}

.detail-primary-button {
  width: 100%;
  min-height: 52px;
  border: 0;
  border-radius: 12px;
  background: #d92828;
  color: #ffffff;
  display: flex;
  align-items: center;
  justify-content: center;
  text-decoration: none;
  font: inherit;
  font-size: 14px;
  font-weight: 800;
  cursor: pointer;
  transition: 0.2s ease;
}

.detail-primary-button:hover:not(:disabled) {
  background: #b91f1f;
  transform: translateY(-1px);
}

.detail-primary-button:disabled {
  background: #c7c7c1;
  cursor: not-allowed;
}

.detail-note {
  display: block;
  margin-top: 14px;
  color: #92928b;
  font-size: 11px;
  line-height: 1.6;
  text-align: center;
}

.detail-alert {
  margin-bottom: 16px;
  padding: 13px 14px;
  border: 1px solid #f0caca;
  border-radius: 10px;
  background: #fff4f4;
  color: #a52222;
  font-size: 12px;
  line-height: 1.5;
}

.detail-success {
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
}

.detail-success-icon {
  width: 46px;
  height: 46px;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 14px;
  border-radius: 50%;
  background: #e8f6ed;
  color: #278451;
  font-size: 20px;
  font-weight: 800;
}

.detail-success strong {
  font-size: 16px;
}

.detail-success span {
  margin: 8px 0 18px;
  color: #777771;
  font-size: 13px;
}

/* STATES */

.detail-state {
  min-height: calc(100vh - 76px);
  padding: 80px 24px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  text-align: center;
}

.detail-state strong {
  margin-top: 18px;
  font-size: 20px;
}

.detail-state span:not(.detail-state-label) {
  margin-top: 7px;
  color: #85857f;
  font-size: 13px;
}

.detail-state p {
  max-width: 500px;
  margin: 12px 0 24px;
  color: #777771;
  line-height: 1.6;
}

.detail-state-label {
  color: #a52222;
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 0.15em;
}

.detail-state-button {
  display: inline-flex;
  min-height: 46px;
  padding: 0 18px;
  align-items: center;
  justify-content: center;
  background: #111111;
  color: #ffffff;
  border-radius: 10px;
  text-decoration: none;
  font-size: 13px;
  font-weight: 700;
}

.detail-spinner {
  width: 30px;
  height: 30px;
  border: 3px solid #deded8;
  border-top-color: #111111;
  border-radius: 50%;
  animation: detail-spin 0.8s linear infinite;
}

@keyframes detail-spin {
  to {
    transform: rotate(360deg);
  }
}

/* FOOTER */

.detail-footer {
  background: #111111;
  color: #ffffff;
  padding: 30px 0;
}

.detail-footer-inner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 30px;
}

.detail-footer-inner > div {
  display: flex;
  flex-direction: column;
  gap: 5px;
}

.detail-footer-inner strong {
  font-size: 14px;
}

.detail-footer-inner span {
  color: #898983;
  font-size: 11px;
}

/* RESPONSIVE */

@media (max-width: 900px) {
  .detail-layout {
    grid-template-columns: 1fr;
    gap: 45px;
  }

  .detail-registration-card {
    position: static;
  }

  .detail-hero h1 {
    font-size: 54px;
  }
}

@media (max-width: 700px) {
  .detail-container {
    width: min(100% - 32px, 1180px);
  }

  .detail-header {
    height: auto;
    padding: 14px 0;
  }

  .detail-nav {
    gap: 12px;
  }

  .detail-nav-link:first-child {
    display: none;
  }

  .detail-nav-button {
    min-height: 38px;
    padding: 0 14px;
  }

  .detail-brand-name {
    display: none;
  }

  .detail-hero {
    padding: 44px 0 54px;
  }

  .detail-hero .detail-back {
    margin-bottom: 38px;
  }

  .detail-hero h1 {
    font-size: 44px;
  }

  .detail-hero p {
    font-size: 15px;
  }

  .detail-content {
    padding: 60px 0;
  }

  .detail-main h2 {
    font-size: 32px;
  }

  .detail-info-grid {
    grid-template-columns: 1fr;
  }

  .detail-registration-card {
    padding: 24px;
  }

  .detail-footer-inner {
    flex-direction: column;
    align-items: flex-start;
  }
}


===== FILE: E:\School Event Planner\frontend\src\styles\Events.css =====

/* ========================================
   EVENTS PAGE
======================================== */

.events-page {
  min-height: 100vh;
  background: #f7f6f2;
  color: #171717;
}

.events-container {
  width: min(1180px, calc(100% - 48px));
  margin: 0 auto;
}

/* ========================================
   HEADER
======================================== */

.events-header {
  position: relative;
  z-index: 20;
  background: #ffffff;
  border-bottom: 1px solid #e8e5df;
}

.events-header-inner {
  min-height: 78px;

  display: flex;
  align-items: center;
  justify-content: space-between;

  gap: 30px;
}

.events-brand {
  display: inline-flex;
  align-items: center;
  gap: 12px;

  color: #171717;
  text-decoration: none;
}

.events-brand-mark {
  width: 42px;
  height: 42px;
  display: flex;
  align-items: center;
  justify-content: center;

  background: #c31320;
  color: #ffffff;

  font-size: 14px;
  font-weight: 800;
  letter-spacing: -0.04em;

  border-radius: 12px;
  flex-shrink: 0;
}

.events-brand-name {
  display: flex;
  flex-direction: column;

  font-size: 15px;
  line-height: 1.05;
  letter-spacing: -0.02em;
}

.events-brand-name strong {
  font-weight: 800;
}

.events-nav {
  display: flex;
  align-items: center;
  gap: 28px;
}

.events-nav a {
  color: #55514b;
  text-decoration: none;

  font-size: 14px;
  font-weight: 600;

  transition:
    color 0.2s ease,
    background 0.2s ease,
    transform 0.2s ease;
}

.events-nav a:hover {
  color: #171717;
}

.events-nav a.active {
  color: #171717;
}

/* Dashboard / Daftar */

.events-nav-cta {
  padding: 11px 18px;

  border-radius: 8px;

  background: #b91c1c;
  color: #ffffff !important;

  font-weight: 700 !important;
}

.events-nav-cta:hover {
  background: #991b1b;
  color: #ffffff !important;

  transform: translateY(-1px);
}

/* ========================================
   HERO
======================================== */

.events-hero {
  position: relative;
  overflow: hidden;

  background: #171717;
  color: #ffffff;
}

.events-hero::before {
  content: "";

  position: absolute;

  width: 520px;
  height: 520px;

  top: -300px;
  right: -120px;

  border: 1px solid rgba(255, 255, 255, 0.07);

  border-radius: 50%;
}

.events-hero::after {
  content: "";

  position: absolute;

  width: 380px;
  height: 380px;

  left: 20%;
  bottom: -290px;

  border: 1px solid rgba(255, 255, 255, 0.05);

  border-radius: 50%;
}

.events-hero-grid {
  position: relative;
  z-index: 2;

  min-height: 390px;

  display: grid;
  grid-template-columns: 1fr auto;

  align-items: center;

  gap: 60px;

  padding-top: 70px;
  padding-bottom: 70px;
}

.events-eyebrow {
  display: inline-block;

  margin-bottom: 16px;

  color: #8a857d;

  font-size: 11px;
  font-weight: 800;

  letter-spacing: 0.16em;
}

.events-eyebrow-light {
  color: rgba(255, 255, 255, 0.55);
}

.events-hero h1 {
  max-width: 720px;

  margin: 0;

  font-size: clamp(46px, 6vw, 78px);
  line-height: 0.95;

  font-weight: 800;

  letter-spacing: -0.055em;
}

.events-hero h1 span {
  color: #d4d0c8;
}

.events-hero p {
  max-width: 590px;

  margin: 26px 0 0;

  color: rgba(255, 255, 255, 0.66);

  font-size: 16px;
  line-height: 1.7;
}

.events-hero-counter {
  min-width: 190px;

  padding-left: 30px;

  border-left: 1px solid rgba(255, 255, 255, 0.16);
}

.events-hero-counter strong {
  display: block;

  font-size: 72px;
  line-height: 0.9;

  font-weight: 800;

  letter-spacing: -0.06em;
}

.events-hero-counter span {
  display: block;

  margin-top: 14px;

  color: rgba(255, 255, 255, 0.55);

  font-size: 13px;
  line-height: 1.5;
}

/* ========================================
   EVENTS BROWSER
======================================== */

.events-browser {
  padding-top: 82px;
  padding-bottom: 100px;
}

.events-section-heading {
  display: flex;

  align-items: flex-end;
  justify-content: space-between;

  gap: 30px;

  margin-bottom: 38px;
}

.events-section-heading h2 {
  max-width: 600px;

  margin: 0;

  font-size: clamp(30px, 4vw, 44px);
  line-height: 1;

  font-weight: 800;

  letter-spacing: -0.045em;
}

/* ========================================
   FILTER
======================================== */

.events-filter-group {
  display: flex;
  align-items: center;

  gap: 5px;

  padding: 5px;

  border: 1px solid #dfdcd5;
  border-radius: 10px;

  background: #ffffff;
}

.events-filter {
  border: 0;
  outline: none;

  padding: 10px 16px;

  border-radius: 7px;

  background: transparent;
  color: #77726a;

  font-family: inherit;

  font-size: 13px;
  font-weight: 700;

  cursor: pointer;

  transition:
    background 0.2s ease,
    color 0.2s ease;
}

.events-filter:hover {
  color: #171717;
}

.events-filter.active {
  background: #171717;
  color: #ffffff;
}

/* ========================================
   EVENTS GRID
======================================== */

.events-grid {
  display: grid;

  grid-template-columns: repeat(3, minmax(0, 1fr));

  gap: 16px;
}

.events-card {
  position: relative;

  min-height: 390px;

  display: flex;
  flex-direction: column;

  padding: 26px;

  overflow: hidden;

  border: 1px solid #e3e0d9;
  border-radius: 14px;

  background: #ffffff;
  color: #171717;

  text-decoration: none;

  transition:
    transform 0.25s ease,
    border-color 0.25s ease,
    box-shadow 0.25s ease;
}

.events-card:hover {
  transform: translateY(-5px);

  border-color: #cbc7bf;

  box-shadow:
    0 18px 45px rgba(23, 23, 23, 0.08);
}

.events-card-top {
  display: flex;

  align-items: center;
  justify-content: space-between;
}

.events-tag {
  display: inline-flex;
  align-items: center;

  min-height: 25px;

  padding: 0 9px;

  border-radius: 5px;

  background: #f0eee9;
  color: #59554f;

  font-size: 10px;
  font-weight: 800;

  letter-spacing: 0.1em;
}

.events-number {
  color: #aaa59d;

  font-size: 12px;
  font-weight: 800;

  letter-spacing: 0.08em;
}

.events-card-body {
  flex: 1;

  padding-top: 65px;
}

.events-card-body h3 {
  max-width: 280px;

  margin: 0;

  font-size: 27px;
  line-height: 1.05;

  font-weight: 800;

  letter-spacing: -0.04em;
}

.events-card-body p {
  max-width: 320px;

  margin: 15px 0 0;

  color: #77726a;

  font-size: 13px;
  line-height: 1.65;

  display: -webkit-box;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 3;

  overflow: hidden;
}

.events-meta {
  display: flex;
  flex-direction: column;

  gap: 12px;

  padding-top: 22px;

  border-top: 1px solid #ebe8e1;
}

.events-meta div {
  display: flex;

  align-items: center;
  justify-content: space-between;

  gap: 15px;
}

.events-meta span {
  color: #aaa59d;

  font-size: 9px;
  font-weight: 800;

  letter-spacing: 0.12em;
}

.events-meta strong {
  max-width: 220px;

  color: #34312d;

  font-size: 11px;
  font-weight: 700;

  text-align: right;
}

.events-card-arrow {
  position: absolute;

  top: 67px;
  right: 24px;

  width: 35px;
  height: 35px;

  display: grid;
  place-items: center;

  border: 1px solid #dedbd4;
  border-radius: 50%;

  color: #171717;

  font-size: 15px;

  transition:
    background 0.2s ease,
    color 0.2s ease,
    transform 0.2s ease;
}

.events-card:hover .events-card-arrow {
  background: #171717;
  color: #ffffff;

  transform: rotate(4deg);
}

/* ========================================
   STATE
======================================== */

.events-state {
  min-height: 300px;

  display: flex;
  flex-direction: column;

  align-items: center;
  justify-content: center;

  padding: 50px 25px;

  border: 1px solid #e2dfd8;
  border-radius: 14px;

  background: #ffffff;

  text-align: center;
}

.events-state strong {
  margin-top: 18px;

  font-size: 18px;
  font-weight: 800;

  letter-spacing: -0.02em;
}

.events-state > span:last-child {
  margin-top: 7px;

  color: #8b867e;

  font-size: 13px;
}

.events-spinner {
  width: 30px;
  height: 30px;

  border: 3px solid #e3e0d9;
  border-top-color: #171717;

  border-radius: 50%;

  animation: events-spin 0.8s linear infinite;
}

@keyframes events-spin {
  to {
    transform: rotate(360deg);
  }
}

/* ========================================
   ERROR
======================================== */

.events-state-error {
  align-items: flex-start;

  text-align: left;
}

.events-state-error h3 {
  margin: 4px 0 0;

  font-size: 24px;

  font-weight: 800;

  letter-spacing: -0.035em;
}

.events-state-error p {
  margin: 10px 0 0;

  color: #77726a;

  font-size: 14px;
  line-height: 1.6;
}

/* ========================================
   EMPTY
======================================== */

.events-state-empty {
  min-height: 330px;
}

.events-empty-icon {
  margin-bottom: 18px;

  color: #aaa59d;

  font-size: 38px;
  font-weight: 300;
}

.events-state-empty h3 {
  margin: 4px 0 0;

  font-size: 24px;

  font-weight: 800;

  letter-spacing: -0.035em;
}

.events-state-empty p {
  margin: 10px 0 0;

  color: #77726a;

  font-size: 14px;
}

/* ========================================
   RESPONSIVE
======================================== */

@media (max-width: 1000px) {
  .events-grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}

@media (max-width: 760px) {
  .events-container {
    width: min(100% - 32px, 600px);
  }

  .events-header-inner {
    min-height: 70px;
  }

  .events-brand-name {
    display: none;
  }

  .events-nav {
    gap: 18px;
  }

  .events-nav a {
    font-size: 12px;
  }

  .events-nav-cta {
    padding: 10px 14px;
  }

  .events-hero-grid {
    grid-template-columns: 1fr;

    min-height: auto;

    gap: 45px;

    padding-top: 60px;
    padding-bottom: 60px;
  }

  .events-hero h1 {
    font-size: clamp(43px, 12vw, 62px);
  }

  .events-hero p {
    max-width: 520px;

    font-size: 14px;
  }

  .events-hero-counter {
    min-width: 0;

    padding-top: 22px;
    padding-left: 0;

    border-top: 1px solid rgba(255, 255, 255, 0.16);
    border-left: 0;
  }

  .events-hero-counter strong {
    font-size: 58px;
  }

  .events-browser {
    padding-top: 60px;
    padding-bottom: 75px;
  }

  .events-section-heading {
    flex-direction: column;

    align-items: flex-start;

    margin-bottom: 28px;
  }

  .events-section-heading h2 {
    font-size: 36px;
  }

  .events-filter-group {
    width: 100%;
  }

  .events-filter {
    flex: 1;
  }

  .events-grid {
    grid-template-columns: 1fr;
  }
}

@media (max-width: 480px) {
  .events-container {
    width: calc(100% - 24px);
  }

  .events-brand-mark {
    width: 38px;
    height: 38px;
  }

  .events-nav {
    gap: 10px;
  }

  .events-nav a:not(.events-nav-cta):not(.active) {
    display: none;
  }

  .events-hero h1 {
    font-size: 43px;
  }

  .events-card {
    min-height: 360px;

    padding: 22px;
  }

  .events-card-body {
    padding-top: 58px;
  }

  .events-card-body h3 {
    font-size: 24px;
  }

  .events-card-arrow {
    top: 62px;
    right: 20px;
  }

  .events-meta strong {
    max-width: 180px;
  }
}


===== FILE: E:\School Event Planner\frontend\src\styles\Global.css =====

@import url("https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&family=Manrope:wght@500;600;700;800&display=swap");

:root {
  --red-900: #8f0d17;
  --red-800: #a70f1b;
  --red-700: #c31320;
  --red-600: #df1f2d;
  --red-100: #fff0f1;
  --red-50: #fff7f7;

  --ink: #15171b;
  --muted: #6e727a;
  --line: #e9e9ec;
  --surface: #ffffff;
  --soft: #f7f7f8;
  --green: #16834b;

  --shadow: 0 20px 60px rgba(21, 23, 27, 0.09);
  --radius: 22px;
}

* {
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

body {
  margin: 0;
  background: var(--surface);
  color: var(--ink);
  font-family: "DM Sans", sans-serif;
}

button,
input,
textarea,
select {
  font: inherit;
}

button,
a {
  -webkit-tap-highlight-color: transparent;
}

a {
  color: inherit;
  text-decoration: none;
}

button {
  cursor: pointer;
}

button:disabled {
  cursor: not-allowed;
  opacity: 0.58;
}

h1,
h2,
h3,
p {
  margin-top: 0;
}

h1,
h2,
h3 {
  font-family: "Manrope", sans-serif;
}


===== FILE: E:\School Event Planner\frontend\src\styles\Home.css =====

.home-page {
  min-height: 100vh;
  background: #ffffff;
  color: #15171b;
  font-family: "DM Sans", sans-serif;
}

.home-page *,
.home-page *::before,
.home-page *::after {
  box-sizing: border-box;
}

.home-page a {
  color: inherit;
  text-decoration: none;
}

.home-container {
  width: min(1180px, calc(100% - 40px));
  margin: 0 auto;
}

/* HEADER */

.home-header {
  min-height: 76px;
  position: relative;
  z-index: 20;
  background: rgba(255, 255, 255, 0.94);
  border-bottom: 1px solid #e9e9ec;
  backdrop-filter: blur(18px);
}

.home-header-inner {
  min-height: 76px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 24px;
}

.home-brand {
  display: inline-flex;
  align-items: center;
  gap: 11px;
  font-family: "Manrope", sans-serif;
  font-weight: 700;
}

.home-brand-mark {
  width: 40px;
  height: 40px;
  display: grid;
  place-items: center;
  flex: 0 0 auto;
  border-radius: 12px;
  color: white;
  background: #c31320;
  font-size: 13px;
  letter-spacing: 0.04em;
  box-shadow: 0 9px 22px rgba(195, 19, 32, 0.24);
}

.home-brand-name {
  color: #15171b;
  line-height: 1.05;
}

.home-brand-name strong {
  display: block;
  color: #c31320;
}

.home-nav {
  display: flex;
  align-items: center;
  gap: 8px;
}

.home-nav > a {
  padding: 10px 14px;
  border-radius: 11px;
  color: #5f636b;
  font-size: 14px;
  transition: 0.2s ease;
}

.home-nav > a:hover {
  color: #c31320;
  background: #fff7f7;
}

.home-nav-cta {
  color: white !important;
  background: #c31320 !important;
  box-shadow: 0 7px 20px rgba(195, 19, 32, 0.18);
}

.home-nav-cta:hover {
  background: #a70f1b !important;
}

/* GENERAL */

.home-eyebrow {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  color: #c31320;
  font-size: 11px;
  font-weight: 800;
  letter-spacing: 0.16em;
}

.home-eyebrow-light {
  color: rgba(255, 255, 255, 0.72);
}

/* HERO */

.home-hero {
  min-height: 650px;
  display: flex;
  align-items: center;
  overflow: hidden;
  color: white;
  background:
    radial-gradient(
      circle at 78% 35%,
      rgba(255, 255, 255, 0.11),
      transparent 25%
    ),
    radial-gradient(
      circle at 15% 90%,
      rgba(255, 255, 255, 0.08),
      transparent 30%
    ),
    linear-gradient(
      135deg,
      #a70f1b 0%,
      #c31320 48%,
      #8f0d17 100%
    );
}

.home-hero-grid {
  min-height: 650px;
  display: grid;
  grid-template-columns: 1.05fr 0.95fr;
  align-items: center;
  gap: 50px;
}

.home-hero-content h1 {
  margin: 14px 0 22px;
  font-family: "Manrope", sans-serif;
  font-size: clamp(46px, 6vw, 76px);
  line-height: 0.98;
  letter-spacing: -0.065em;
}

.home-hero-content h1 span {
  color: #ffdfe2;
}

.home-hero-content p {
  max-width: 610px;
  margin: 0 0 30px;
  color: rgba(255, 255, 255, 0.76);
  font-size: 17px;
  line-height: 1.75;
}

.home-hero-actions {
  display: flex;
  flex-wrap: wrap;
  gap: 11px;
}

.home-btn {
  min-height: 48px;
  padding: 0 20px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 12px;
  border-radius: 13px;
  font-weight: 800;
  transition: 0.2s ease;
}

.home-btn:hover {
  transform: translateY(-2px);
}

.home-btn-white {
  color: #a70f1b;
  background: white;
  box-shadow: 0 12px 28px rgba(60, 0, 5, 0.14);
}

.home-btn-outline {
  color: white;
  border: 1px solid rgba(255, 255, 255, 0.28);
  background: transparent;
}

.home-btn-outline:hover {
  background: rgba(255, 255, 255, 0.08);
}

.home-trust {
  display: flex;
  align-items: center;
  gap: 9px;
  margin-top: 25px;
  color: rgba(255, 255, 255, 0.66);
  font-size: 13px;
}

.home-pulse-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: #ffd6d9;
  box-shadow: 0 0 0 5px rgba(255, 214, 217, 0.1);
}

/* HERO VISUAL */

.home-hero-visual {
  min-height: 500px;
  position: relative;
  display: grid;
  place-items: center;
}

.home-orb {
  position: absolute;
  border: 1px solid rgba(255, 255, 255, 0.13);
  border-radius: 50%;
}

.home-orb-one {
  width: 440px;
  height: 440px;
}

.home-orb-two {
  width: 310px;
  height: 310px;
}

.home-event-poster {
  width: min(340px, 72%);
  min-height: 410px;
  padding: 24px;
  position: relative;
  z-index: 2;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  border: 1px solid rgba(255, 255, 255, 0.2);
  border-radius: 25px;
  background: rgba(255, 255, 255, 0.12);
  box-shadow: 0 35px 80px rgba(60, 0, 5, 0.28);
  backdrop-filter: blur(15px);
  transform: rotate(3deg);
}

.home-poster-top,
.home-poster-bottom {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 15px;
  color: rgba(255, 255, 255, 0.68);
  font-size: 11px;
  font-weight: 800;
  letter-spacing: 0.13em;
}

.home-poster-status {
  padding: 6px 9px;
  border-radius: 20px;
  color: white;
  background: rgba(255, 255, 255, 0.13);
}

.home-poster-day {
  margin-top: 10px;
  font-family: "Manrope", sans-serif;
  font-size: 130px;
  line-height: 0.9;
  font-weight: 800;
  letter-spacing: -0.09em;
}

.home-poster-title {
  padding: 25px 0;
  border-top: 1px solid rgba(255, 255, 255, 0.18);
}

.home-poster-title span {
  display: block;
  margin-bottom: 7px;
  color: #ffd4d7;
  font-size: 11px;
  font-weight: 800;
  letter-spacing: 0.16em;
}

.home-poster-title strong {
  font-family: "Manrope", sans-serif;
  font-size: 29px;
  line-height: 1.04;
}

.home-floating-card {
  position: absolute;
  z-index: 3;
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 15px;
  border: 1px solid rgba(255, 255, 255, 0.2);
  border-radius: 16px;
  background: rgba(255, 255, 255, 0.13);
  box-shadow: 0 20px 50px rgba(60, 0, 5, 0.22);
  backdrop-filter: blur(15px);
}

.home-floating-events {
  top: 15%;
  right: 1%;
}

.home-floating-events strong {
  font-size: 29px;
}

.home-floating-events span,
.home-floating-new small {
  color: rgba(255, 255, 255, 0.62);
  font-size: 11px;
}

.home-floating-new {
  left: 1%;
  bottom: 14%;
}

.home-floating-plus {
  width: 38px;
  height: 38px;
  display: grid;
  place-items: center;
  border-radius: 12px;
  color: #c31320;
  background: white;
  font-size: 22px;
}

.home-floating-new strong {
  display: block;
  font-size: 13px;
}

/* STATS */

.home-stats {
  border-bottom: 1px solid #e9e9ec;
  background: white;
}

.home-stats-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
}

.home-stats-grid > div {
  padding: 28px 25px;
  border-right: 1px solid #e9e9ec;
}

.home-stats-grid > div:first-child {
  border-left: 1px solid #e9e9ec;
}

.home-stats-grid strong {
  display: block;
  margin-bottom: 3px;
  font-family: "Manrope", sans-serif;
  font-size: 28px;
}

.home-stats-grid span {
  color: #6e727a;
  font-size: 13px;
}

/* EVENTS */

.home-events {
  padding-top: 105px;
  padding-bottom: 110px;
}

.home-section-heading {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: 24px;
  margin-bottom: 30px;
}

.home-section-heading h2 {
  max-width: 650px;
  margin: 8px 0 0;
  font-family: "Manrope", sans-serif;
  font-size: clamp(26px, 3vw, 36px);
  letter-spacing: -0.04em;
}

.home-inline-link {
  color: #c31320;
  font-size: 14px;
  font-weight: 800;
}

.home-inline-link:hover {
  text-decoration: underline;
}

.home-event-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 18px;
}

.home-event-card {
  min-height: 345px;
  padding: 24px;
  position: relative;
  overflow: hidden;
  border: 1px solid #e9e9ec;
  border-radius: 22px;
  background: white;
  transition: 0.25s ease;
}

.home-event-card:hover {
  border-color: #efc4c7;
  box-shadow: 0 20px 60px rgba(21, 23, 27, 0.09);
  transform: translateY(-5px);
}

.home-event-card-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.home-tag {
  display: inline-flex;
  width: fit-content;
  padding: 6px 9px;
  border-radius: 8px;
  color: #c31320;
  background: #fff0f1;
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 0.08em;
}

.home-event-arrow {
  color: #c31320;
  font-size: 20px;
}

.home-event-index {
  display: block;
  margin: 42px 0 10px;
  color: #b9bcc2;
  font-size: 11px;
  font-weight: 800;
  letter-spacing: 0.1em;
}

.home-event-card h3 {
  margin: 0 0 9px;
  font-family: "Manrope", sans-serif;
  font-size: 25px;
  letter-spacing: -0.035em;
}

.home-event-card p {
  margin: 0;
  color: #6e727a;
  line-height: 1.65;
}

.home-event-footer {
  position: absolute;
  right: 24px;
  bottom: 24px;
  left: 24px;
  display: flex;
  justify-content: space-between;
  gap: 10px;
  padding-top: 16px;
  border-top: 1px solid #e9e9ec;
  color: #62666e;
  font-size: 12px;
}

/* STATES */

.home-state {
  min-height: 260px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  gap: 8px;
  padding: 30px;
  text-align: center;
  border: 1px solid #e9e9ec;
  border-radius: 22px;
  background: white;
  color: #6e727a;
}

.home-state strong {
  color: #15171b;
  font-family: "Manrope", sans-serif;
}

.home-state h3 {
  margin: 8px 0 0;
  color: #15171b;
  font-family: "Manrope", sans-serif;
  font-size: 23px;
}

.home-state p {
  max-width: 500px;
  margin: 4px 0 0;
  line-height: 1.6;
}

.home-state-error {
  align-items: flex-start;
  justify-content: center;
  text-align: left;
  padding: 40px;
}

.home-spinner {
  width: 34px;
  height: 34px;
  margin-bottom: 5px;
  border: 3px solid #f0d6d8;
  border-top-color: #c31320;
  border-radius: 50%;
  animation: home-spin 0.8s linear infinite;
}

@keyframes home-spin {
  to {
    transform: rotate(360deg);
  }
}

/* CTA */

.home-cta {
  padding: 82px 0;
  color: white;
  background: #c31320;
}

.home-cta-inner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 40px;
}

.home-cta h2 {
  margin: 10px 0 0;
  font-family: "Manrope", sans-serif;
  font-size: clamp(32px, 4vw, 50px);
  line-height: 1.05;
  letter-spacing: -0.055em;
}

.home-cta-button {
  min-width: 165px;
  min-height: 165px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  gap: 9px;
  border: 1px solid rgba(255, 255, 255, 0.3);
  border-radius: 50%;
  color: white;
  background: rgba(255, 255, 255, 0.09);
  transition: 0.25s ease;
}

.home-cta-button:hover {
  color: #c31320;
  background: white;
  transform: rotate(-4deg);
}

.home-cta-button span {
  max-width: 90px;
  text-align: center;
  font-size: 13px;
  font-weight: 800;
}

/* FOOTER */

.home-footer {
  padding: 25px 0;
  color: #858990;
  background: #121316;
}

.home-footer-inner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 25px;
  font-size: 12px;
}

.home-footer-brand {
  display: flex;
  align-items: center;
  gap: 11px;
}

.home-footer-brand .home-brand-mark {
  width: 34px;
  height: 34px;
  font-size: 11px;
  box-shadow: none;
}

.home-footer-brand strong,
.home-footer-brand span {
  display: block;
}

.home-footer-brand strong {
  margin-bottom: 3px;
  color: white;
}

/* RESPONSIVE */

@media (max-width: 900px) {
  .home-hero {
    min-height: auto;
  }

  .home-hero-grid {
    min-height: auto;
    grid-template-columns: 1fr;
    padding-top: 80px;
    padding-bottom: 80px;
  }

  .home-hero-visual {
    min-height: 430px;
  }

  .home-stats-grid {
    grid-template-columns: repeat(2, 1fr);
  }

  .home-stats-grid > div {
    border-bottom: 1px solid #e9e9ec;
  }

  .home-stats-grid > div:nth-child(odd) {
    border-left: 1px solid #e9e9ec;
  }

  .home-event-grid {
    grid-template-columns: 1fr 1fr;
  }
}

@media (max-width: 720px) {
  .home-container {
    width: min(100% - 28px, 1180px);
  }

  .home-header,
  .home-header-inner {
    min-height: 68px;
  }

  .home-nav {
    gap: 2px;
  }

  .home-nav > a:not(.home-nav-cta) {
    display: none;
  }

  .home-nav-cta {
    padding: 9px 12px !important;
    font-size: 12px;
  }

  .home-brand-mark {
    width: 36px;
    height: 36px;
  }

  .home-brand-name {
    font-size: 13px;
  }

  .home-hero-content h1 {
    font-size: 48px;
  }

  .home-hero-content p {
    font-size: 15px;
  }

  .home-hero-actions {
    display: grid;
  }

  .home-btn {
    width: 100%;
  }

  .home-hero-visual {
    min-height: 390px;
  }

  .home-event-poster {
    width: 260px;
    min-height: 340px;
  }

  .home-poster-day {
    font-size: 105px;
  }

  .home-floating-events {
    right: 0;
  }

  .home-floating-new {
    left: 0;
  }

  .home-events {
    padding-top: 70px;
    padding-bottom: 75px;
  }

  .home-section-heading,
  .home-cta-inner {
    align-items: flex-start;
    flex-direction: column;
  }

  .home-event-grid {
    grid-template-columns: 1fr;
  }

  .home-cta-button {
    min-width: 130px;
    min-height: 130px;
  }

  .home-footer-inner {
    align-items: flex-start;
    flex-direction: column;
  }
}

@media (max-width: 460px) {
  .home-stats-grid {
    grid-template-columns: 1fr;
  }

  .home-event-poster {
    width: 230px;
  }

  .home-floating-card {
    transform: scale(0.9);
  }

  .home-floating-events {
    right: -12px;
  }

  .home-floating-new {
    left: -12px;
  }
}


===== FILE: E:\School Event Planner\frontend\src\styles\Login.css =====

.login-page {
  min-height: 100vh;
  display: grid;
  grid-template-columns: 0.9fr 1.1fr;
  background: #f7f7f8;
  color: #15171b;
}

/* BRAND PANEL */

.login-brand-panel {
  min-height: 100vh;
  padding: 42px;
  display: flex;
  flex-direction: column;
  color: white;
  overflow: hidden;
  position: relative;
  background:
    radial-gradient(
      circle at 80% 20%,
      rgba(255, 255, 255, 0.13),
      transparent 25%
    ),
    radial-gradient(
      circle at 15% 85%,
      rgba(255, 255, 255, 0.08),
      transparent 30%
    ),
    linear-gradient(
      145deg,
      #8f0d17 0%,
      #c31320 55%,
      #870b15 100%
    );
}

.login-brand-panel::before,
.login-brand-panel::after {
  content: "";
  position: absolute;
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: 50%;
  pointer-events: none;
}

.login-brand-panel::before {
  width: 420px;
  height: 420px;
  right: -180px;
  bottom: -120px;
}

.login-brand-panel::after {
  width: 260px;
  height: 260px;
  right: -90px;
  bottom: -40px;
}

.login-brand {
  position: relative;
  z-index: 1;
  display: inline-flex;
  align-items: center;
  gap: 11px;
  width: fit-content;
  color: white;
  font-family: "Manrope", sans-serif;
}

.login-brand-mark {
  width: 40px;
  height: 40px;
  display: grid;
  place-items: center;
  border-radius: 12px;
  color: #c31320;
  background: white;
  font-size: 13px;
  font-weight: 800;
}

.login-brand strong,
.login-brand small {
  display: block;
}

.login-brand strong {
  font-size: 14px;
}

.login-brand small {
  margin-top: 2px;
  color: rgba(255, 255, 255, 0.6);
  font-size: 11px;
}

.login-brand-content {
  position: relative;
  z-index: 1;
  max-width: 530px;
  margin: auto 0;
}

.login-eyebrow {
  display: inline-flex;
  align-items: center;
  color: rgba(255, 255, 255, 0.68);
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 0.16em;
}

.login-eyebrow.dark {
  color: #c31320;
}

.login-brand-content h1 {
  margin: 16px 0 22px;
  font-family: "Manrope", sans-serif;
  font-size: clamp(43px, 5vw, 68px);
  line-height: 0.98;
  letter-spacing: -0.07em;
}

.login-brand-content h1 span {
  color: #ffdce0;
}

.login-brand-content p {
  max-width: 470px;
  margin: 0;
  color: rgba(255, 255, 255, 0.7);
  font-size: 15px;
  line-height: 1.8;
}

.login-brand-footer {
  position: relative;
  z-index: 1;
  display: flex;
  justify-content: space-between;
  color: rgba(255, 255, 255, 0.45);
  font-size: 9px;
  font-weight: 800;
  letter-spacing: 0.15em;
}

/* FORM PANEL */

.login-form-panel {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 50px;
  background: white;
}

.login-form-box {
  width: min(430px, 100%);
}

.login-heading {
  margin-bottom: 30px;
}

.login-heading h2 {
  margin: 10px 0 8px;
  font-family: "Manrope", sans-serif;
  font-size: 38px;
  line-height: 1.05;
  letter-spacing: -0.055em;
}

.login-heading p {
  margin: 0;
  color: #6e727a;
  font-size: 14px;
}

.login-mobile-mark {
  display: none;
}

/* ALERT */

.login-alert {
  display: flex;
  flex-direction: column;
  gap: 3px;
  margin-bottom: 18px;
  padding: 13px 15px;
  border: 1px solid #ffd2d6;
  border-radius: 13px;
  color: #8e151d;
  background: #fff0f1;
  font-size: 12px;
  line-height: 1.5;
}

.login-alert strong {
  font-weight: 800;
}

/* FORM */

.login-form {
  display: grid;
  gap: 18px;
}

.login-field {
  display: grid;
  gap: 7px;
}

.login-field label {
  font-size: 12px;
  font-weight: 800;
}

.login-field input {
  width: 100%;
  min-height: 49px;
  padding: 13px 14px;
  border: 1px solid #dedfe3;
  border-radius: 12px;
  outline: none;
  color: #15171b;
  background: white;
  transition:
    border-color 0.2s ease,
    box-shadow 0.2s ease;
}

.login-field input::placeholder {
  color: #afb2b8;
}

.login-field input:focus {
  border-color: #df1f2d;
  box-shadow: 0 0 0 4px rgba(223, 31, 45, 0.08);
}

.login-label-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

/* BUTTON */

.login-submit {
  min-height: 50px;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
  margin-top: 4px;
  padding: 0 20px;
  border: 0;
  border-radius: 13px;
  color: white;
  background: #c31320;
  box-shadow: 0 12px 28px rgba(195, 19, 32, 0.2);
  font-weight: 800;
  transition:
    background 0.2s ease,
    transform 0.2s ease,
    box-shadow 0.2s ease;
}

.login-submit:hover:not(:disabled) {
  background: #a70f1b;
  transform: translateY(-2px);
  box-shadow: 0 16px 32px rgba(195, 19, 32, 0.25);
}

.login-submit:disabled {
  cursor: not-allowed;
  opacity: 0.65;
}

.login-button-spinner {
  width: 17px;
  height: 17px;
  border: 2px solid rgba(255, 255, 255, 0.35);
  border-top-color: white;
  border-radius: 50%;
  animation: login-spin 0.7s linear infinite;
}

@keyframes login-spin {
  to {
    transform: rotate(360deg);
  }
}

/* BOTTOM */

.login-register {
  display: flex;
  justify-content: center;
  gap: 5px;
  margin-top: 23px;
  color: #6e727a;
  font-size: 12px;
}

.login-register a {
  color: #c31320;
  font-weight: 800;
}

.login-register a:hover {
  text-decoration: underline;
}

.login-back {
  display: block;
  width: fit-content;
  margin: 26px auto 0;
  color: #6e727a;
  font-size: 12px;
  font-weight: 700;
}

.login-back:hover {
  color: #c31320;
}

/* RESPONSIVE */

@media (max-width: 900px) {
  .login-page {
    grid-template-columns: 1fr;
  }

  .login-brand-panel {
    min-height: 430px;
  }

  .login-form-panel {
    min-height: auto;
    padding: 65px 30px;
  }
}

@media (max-width: 600px) {
  .login-brand-panel {
    min-height: 360px;
    padding: 28px 22px;
  }

  .login-brand-content {
    margin-top: auto;
    margin-bottom: 45px;
  }

  .login-brand-content h1 {
    font-size: 43px;
  }

  .login-brand-content p {
    font-size: 13px;
  }

  .login-brand-footer {
    font-size: 8px;
  }

  .login-form-panel {
    padding: 45px 20px;
  }

  .login-form-box {
    width: 100%;
  }

  .login-mobile-mark {
    width: 43px;
    height: 43px;
    display: grid;
    place-items: center;
    margin-bottom: 30px;
    border-radius: 13px;
    color: white;
    background: #c31320;
    font-family: "Manrope", sans-serif;
    font-weight: 800;
  }

  .login-heading h2 {
    font-size: 32px;
  }
}


===== FILE: E:\School Event Planner\frontend\src\styles\Register.css =====

.register-page {
  min-height: 100vh;
  display: grid;
  grid-template-columns: 0.9fr 1.1fr;
  background: #f7f7f8;
}

.register-brand {
  min-height: 100vh;
  padding: 42px;
  display: flex;
  flex-direction: column;
  color: white;
  background:
    radial-gradient(
      circle at 75% 25%,
      rgba(255, 255, 255, 0.13),
      transparent 26%
    ),
    radial-gradient(
      circle at 15% 85%,
      rgba(255, 255, 255, 0.08),
      transparent 28%
    ),
    linear-gradient(
      145deg,
      #9d0d18,
      #c31320 55%,
      #870b15
    );
}

.register-brand-logo {
  display: inline-flex;
  align-items: center;
  gap: 11px;
  width: fit-content;
  color: white;
  font-family: "Manrope", sans-serif;
}

.register-brand-mark {
  width: 40px;
  height: 40px;
  display: grid;
  place-items: center;
  border-radius: 12px;
  color: #c31320;
  background: white;
  font-size: 13px;
  font-weight: 800;
}

.register-brand-logo strong,
.register-brand-logo small {
  display: block;
}

.register-brand-logo strong {
  font-size: 15px;
}

.register-brand-logo small {
  margin-top: 2px;
  color: rgba(255, 255, 255, 0.65);
  font-size: 11px;
  font-weight: 600;
}

.register-brand-content {
  max-width: 540px;
  margin: auto 0;
}

.register-eyebrow,
.register-form-eyebrow {
  display: inline-flex;
  color: #ffdce0;
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 0.16em;
}

.register-brand-content h1 {
  margin: 16px 0 20px;
  font-family: "Manrope", sans-serif;
  font-size: clamp(43px, 5vw, 68px);
  line-height: 0.98;
  letter-spacing: -0.07em;
}

.register-brand-content h1 span {
  color: #ffdce0;
}

.register-brand-content p {
  max-width: 470px;
  margin: 0;
  color: rgba(255, 255, 255, 0.72);
  line-height: 1.75;
  font-size: 15px;
}

.register-brand-footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 20px;
  color: rgba(255, 255, 255, 0.5);
  font-size: 9px;
  font-weight: 800;
  letter-spacing: 0.14em;
}

.register-form-side {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 45px;
  background: white;
}

.register-form-box {
  width: min(440px, 100%);
}

.register-form-eyebrow {
  color: #c31320;
}

.register-form-box h2 {
  margin: 10px 0 8px;
  color: #15171b;
  font-family: "Manrope", sans-serif;
  font-size: 38px;
  letter-spacing: -0.055em;
}

.register-subtitle {
  margin: 0 0 30px;
  color: #6e727a;
  font-size: 14px;
  line-height: 1.6;
}

.register-alert {
  display: flex;
  align-items: flex-start;
  gap: 10px;
  margin-bottom: 18px;
  padding: 12px 14px;
  border: 1px solid #ffd4d7;
  border-radius: 12px;
  color: #8e151d;
  background: #fff0f1;
  font-size: 12px;
  line-height: 1.5;
}

.register-alert strong {
  width: 18px;
  height: 18px;
  display: grid;
  flex: 0 0 auto;
  place-items: center;
  border-radius: 50%;
  color: white;
  background: #c31320;
  font-size: 11px;
}

.register-form {
  display: grid;
  gap: 17px;
}

.register-field {
  display: grid;
  gap: 7px;
}

.register-field label {
  color: #15171b;
  font-size: 12px;
  font-weight: 800;
}

.register-field input {
  width: 100%;
  min-height: 48px;
  padding: 13px 14px;
  border: 1px solid #dedfe3;
  outline: none;
  border-radius: 12px;
  color: #15171b;
  background: white;
  font-size: 14px;
  transition:
    border-color 0.2s ease,
    box-shadow 0.2s ease;
}

.register-field input::placeholder {
  color: #afb2b8;
}

.register-field input:focus {
  border-color: #df1f2d;
  box-shadow: 0 0 0 4px rgba(223, 31, 45, 0.08);
}

.register-submit {
  width: 100%;
  min-height: 49px;
  margin-top: 5px;
  border: 0;
  border-radius: 13px;
  color: white;
  background: #c31320;
  font-size: 14px;
  font-weight: 800;
  box-shadow: 0 12px 28px rgba(195, 19, 32, 0.18);
  transition:
    transform 0.2s ease,
    background 0.2s ease;
}

.register-submit:hover:not(:disabled) {
  transform: translateY(-2px);
  background: #a70f1b;
}

.register-submit:disabled {
  cursor: not-allowed;
  opacity: 0.6;
}

.register-switch {
  display: flex;
  justify-content: center;
  gap: 5px;
  margin-top: 23px;
  color: #6e727a;
  font-size: 12px;
}

.register-switch a,
.register-back {
  color: #c31320;
  font-weight: 800;
}

.register-switch a:hover,
.register-back:hover {
  text-decoration: underline;
}

.register-back {
  display: block;
  width: fit-content;
  margin: 24px auto 0;
  font-size: 12px;
}

.register-mobile-logo {
  display: none;
}

/* RESPONSIVE */

@media (max-width: 900px) {
  .register-page {
    grid-template-columns: 1fr;
  }

  .register-brand {
    min-height: 420px;
  }

  .register-form-side {
    min-height: auto;
    padding: 60px 30px;
  }
}

@media (max-width: 720px) {
  .register-brand {
    min-height: 350px;
    padding: 28px 22px;
  }

  .register-brand-content h1 {
    font-size: 45px;
  }

  .register-brand-content p {
    font-size: 14px;
  }

  .register-brand-footer {
    margin-top: 45px;
  }

  .register-form-side {
    padding: 45px 20px;
  }

  .register-form-box h2 {
    font-size: 31px;
  }

  .register-mobile-logo {
    width: 43px;
    height: 43px;
    display: grid;
    place-items: center;
    margin-bottom: 30px;
    border-radius: 13px;
    color: white;
    background: #c31320;
    font-family: "Manrope", sans-serif;
    font-weight: 800;
  }
}


===== FILE: E:\School Event Planner\frontend\src\styles\StaffDashboard.css =====

.staff-page {
  min-height: 100vh;
  background: #ffffff;
  color: #15171b;
  font-family: "DM Sans", sans-serif;
}

.staff-page *,
.staff-page *::before,
.staff-page *::after {
  box-sizing: border-box;
}

.staff-page a {
  color: inherit;
  text-decoration: none;
}

.staff-container {
  width: min(1180px, calc(100% - 40px));
  margin: 0 auto;
}


/* =========================
   HEADER
========================= */

.staff-header {
  min-height: 76px;
  position: relative;
  z-index: 20;
  background: rgba(255, 255, 255, 0.94);
  border-bottom: 1px solid #e9e9ec;
  backdrop-filter: blur(18px);
}

.staff-header-inner {
  min-height: 76px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 24px;
}

.staff-brand {
  display: inline-flex;
  align-items: center;
  gap: 11px;
  font-family: "Manrope", sans-serif;
  font-weight: 700;
}

.staff-brand-mark {
  width: 40px;
  height: 40px;
  display: grid;
  place-items: center;
  flex: 0 0 auto;
  border-radius: 12px;
  color: #ffffff;
  background: #c31320;
  font-size: 13px;
  letter-spacing: 0.04em;
  box-shadow: 0 9px 22px rgba(195, 19, 32, 0.24);
}

.staff-brand-name {
  color: #15171b;
  line-height: 1.05;
}

.staff-brand-name strong {
  display: block;
  color: #c31320;
}

.staff-nav {
  display: flex;
  align-items: center;
  gap: 8px;
}

.staff-nav > a {
  padding: 10px 14px;
  border-radius: 11px;
  color: #5f636b;
  font-size: 14px;
  transition: 0.2s ease;
}

.staff-nav > a:hover {
  color: #c31320;
  background: #fff7f7;
}

.staff-nav > a.active {
  color: #c31320;
  background: #fff0f1;
  font-weight: 800;
}

.staff-role {
  padding: 7px 10px;
  border-radius: 8px;
  color: #c31320;
  background: #fff0f1;
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 0.1em;
}

.staff-logout {
  padding: 10px 14px;
  border: 0;
  border-radius: 11px;
  color: #5f636b;
  background: transparent;
  font-family: inherit;
  font-size: 14px;
  cursor: pointer;
  transition: 0.2s ease;
}

.staff-logout:hover {
  color: #c31320;
  background: #fff7f7;
}


/* =========================
   GENERAL
========================= */

.staff-eyebrow {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  color: #c31320;
  font-size: 11px;
  font-weight: 800;
  letter-spacing: 0.16em;
}


/* =========================
   HERO
========================= */

.staff-hero {
  min-height: 300px;
  padding-top: 70px;
  padding-bottom: 60px;
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: 40px;
}

.staff-hero-content h1 {
  margin: 12px 0 14px;
  font-family: "Manrope", sans-serif;
  font-size: clamp(42px, 5vw, 64px);
  line-height: 0.98;
  letter-spacing: -0.06em;
}

.staff-hero-content p {
  max-width: 590px;
  margin: 0;
  color: #6e727a;
  font-size: 16px;
  line-height: 1.7;
}

.staff-primary-button {
  min-height: 48px;
  padding: 0 19px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 9px;
  flex-shrink: 0;
  border: 0;
  border-radius: 13px;
  color: #ffffff;
  background: #c31320;
  box-shadow: 0 10px 25px rgba(195, 19, 32, 0.2);
  font-family: inherit;
  font-size: 14px;
  font-weight: 800;
  cursor: pointer;
  transition: 0.2s ease;
}

.staff-primary-button:hover {
  background: #a70f1b;
  transform: translateY(-2px);
}

.staff-primary-button:disabled {
  opacity: 0.6;
  cursor: not-allowed;
  transform: none;
}

.staff-primary-button span {
  font-size: 20px;
  line-height: 1;
}


/* =========================
   STATS
========================= */

.staff-stats {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  border-top: 1px solid #e9e9ec;
  border-bottom: 1px solid #e9e9ec;
}

.staff-stat {
  min-height: 145px;
  padding: 25px;
  border-right: 1px solid #e9e9ec;
  background: #ffffff;
}

.staff-stat:first-child {
  border-left: 1px solid #e9e9ec;
}

.staff-stat.staff-stat-primary {
  background: #fff7f7;
}

.staff-stat > span {
  display: block;
  margin-bottom: 12px;
  color: #777b83;
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 0.13em;
}

.staff-stat-primary > span {
  color: #c31320;
}

.staff-stat strong {
  display: block;
  margin-bottom: 4px;
  font-family: "Manrope", sans-serif;
  font-size: 36px;
  line-height: 1;
  letter-spacing: -0.05em;
}

.staff-stat small {
  color: #858990;
  font-size: 12px;
}


/* =========================
   CONTENT
========================= */

.staff-content {
  padding-top: 90px;
  padding-bottom: 110px;
}

.staff-alert {
  min-height: 54px;
  margin-bottom: 25px;
  padding: 13px 16px;
  display: flex;
  align-items: center;
  gap: 12px;
  border: 1px solid #f0c9cc;
  border-radius: 13px;
  color: #8f0d17;
  background: #fff5f5;
  font-size: 13px;
}

.staff-alert strong {
  width: 27px;
  height: 27px;
  display: grid;
  place-items: center;
  flex: 0 0 auto;
  border-radius: 50%;
  color: #ffffff;
  background: #c31320;
  font-size: 13px;
}

.staff-section-heading {
  margin-bottom: 28px;
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: 20px;
}

.staff-section-heading h2 {
  margin: 8px 0 0;
  font-family: "Manrope", sans-serif;
  font-size: 34px;
  letter-spacing: -0.045em;
}

.staff-result-count {
  color: #858990;
  font-size: 13px;
}


/* =========================
   STATE
========================= */

.staff-state {
  min-height: 270px;
  padding: 35px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  gap: 8px;
  text-align: center;
  border: 1px solid #e9e9ec;
  border-radius: 22px;
  background: #ffffff;
  color: #6e727a;
}

.staff-state strong {
  color: #15171b;
  font-family: "Manrope", sans-serif;
}

.staff-state span {
  font-size: 13px;
}

.staff-spinner {
  width: 34px;
  height: 34px;
  margin-bottom: 5px;
  border: 3px solid #f0d6d8;
  border-top-color: #c31320;
  border-radius: 50%;
  animation: staff-spin 0.8s linear infinite;
}

@keyframes staff-spin {
  to {
    transform: rotate(360deg);
  }
}


/* =========================
   EMPTY
========================= */

.staff-empty {
  min-height: 360px;
  padding: 45px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  text-align: center;
  border: 1px solid #e9e9ec;
  border-radius: 22px;
  background: #ffffff;
}

.staff-empty-icon {
  width: 58px;
  height: 58px;
  margin-bottom: 20px;
  display: grid;
  place-items: center;
  border-radius: 17px;
  color: #c31320;
  background: #fff0f1;
  font-size: 30px;
  font-weight: 400;
}

.staff-empty h3 {
  margin: 9px 0 6px;
  font-family: "Manrope", sans-serif;
  font-size: 24px;
  letter-spacing: -0.03em;
}

.staff-empty p {
  max-width: 440px;
  margin: 0 0 23px;
  color: #6e727a;
  line-height: 1.6;
}


/* =========================
   EVENT LIST
========================= */

.staff-event-list {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.staff-event-card {
  min-height: 120px;
  padding: 21px 22px;
  display: grid;
  grid-template-columns: 45px minmax(0, 1fr) 125px auto;
  align-items: center;
  gap: 22px;
  border: 1px solid #e9e9ec;
  border-radius: 18px;
  background: #ffffff;
  transition: 0.25s ease;
}

.staff-event-card:hover {
  border-color: #efc4c7;
  box-shadow: 0 18px 45px rgba(21, 23, 27, 0.07);
  transform: translateY(-2px);
}

.staff-event-number {
  color: #b9bcc2;
  font-size: 12px;
  font-weight: 800;
  letter-spacing: 0.1em;
}

.staff-event-info {
  min-width: 0;
}

.staff-tag-row {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 7px;
  margin-bottom: 8px;
}

.staff-tag {
  padding: 5px 8px;
  border-radius: 7px;
  color: #c31320;
  background: #fff0f1;
  font-size: 9px;
  font-weight: 800;
  letter-spacing: 0.08em;
}

.staff-status {
  padding: 5px 8px;
  border-radius: 7px;
  font-size: 9px;
  font-weight: 700;
}

.staff-status.open {
  color: #39734a;
  background: #eef8f1;
}

.staff-status.closed {
  color: #777b83;
  background: #f1f2f3;
}

.staff-event-info h3 {
  margin: 0 0 6px;
  font-family: "Manrope", sans-serif;
  font-size: 20px;
  letter-spacing: -0.03em;
}

.staff-event-info p {
  margin: 0;
  overflow: hidden;
  color: #73777f;
  font-size: 12px;
  line-height: 1.5;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.staff-event-limit {
  padding-left: 18px;
  border-left: 1px solid #e9e9ec;
}

.staff-event-limit span {
  display: block;
  margin-bottom: 6px;
  color: #858990;
  font-size: 9px;
  font-weight: 800;
  letter-spacing: 0.1em;
}

.staff-event-limit strong {
  font-family: "Manrope", sans-serif;
  font-size: 22px;
}

.staff-event-actions {
  display: flex;
  align-items: center;
  gap: 6px;
}

.staff-event-actions button {
  min-height: 37px;
  padding: 0 12px;
  border: 1px solid #e2e3e6;
  border-radius: 10px;
  color: #555960;
  background: #ffffff;
  font-family: inherit;
  font-size: 12px;
  font-weight: 700;
  cursor: pointer;
  transition: 0.2s ease;
}

.staff-event-actions button:hover {
  color: #c31320;
  border-color: #efc4c7;
  background: #fff7f7;
}

.staff-event-actions button.danger {
  color: #b11a25;
}

.staff-event-actions button.danger:hover {
  color: #ffffff;
  border-color: #c31320;
  background: #c31320;
}


/* =========================
   MODAL
========================= */

.staff-modal-backdrop {
  position: fixed;
  inset: 0;
  z-index: 100;
  padding: 30px 20px;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow-y: auto;
  background: rgba(18, 19, 22, 0.58);
  backdrop-filter: blur(7px);
}

.staff-modal {
  width: min(650px, 100%);
  max-height: calc(100vh - 60px);
  overflow-y: auto;
  border: 1px solid #e9e9ec;
  border-radius: 24px;
  background: #ffffff;
  box-shadow: 0 35px 90px rgba(0, 0, 0, 0.22);
  animation: staff-modal-in 0.2s ease;
}

@keyframes staff-modal-in {
  from {
    opacity: 0;
    transform: translateY(10px) scale(0.98);
  }

  to {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

.staff-modal-header {
  padding: 25px 27px;
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 20px;
  border-bottom: 1px solid #e9e9ec;
}

.staff-modal-header h2 {
  margin: 7px 0 0;
  font-family: "Manrope", sans-serif;
  font-size: 27px;
  letter-spacing: -0.04em;
}

.staff-modal-close {
  width: 36px;
  height: 36px;
  display: grid;
  place-items: center;
  flex: 0 0 auto;
  border: 0;
  border-radius: 10px;
  color: #6e727a;
  background: #f5f5f6;
  font-size: 22px;
  line-height: 1;
  cursor: pointer;
  transition: 0.2s ease;
}

.staff-modal-close:hover {
  color: #c31320;
  background: #fff0f1;
}


/* =========================
   FORM
========================= */

.staff-form {
  padding: 27px;
}

.staff-field {
  margin-bottom: 18px;
}

.staff-field label {
  display: block;
  margin-bottom: 8px;
  color: #3d4046;
  font-size: 12px;
  font-weight: 800;
}

.staff-field input,
.staff-field textarea,
.staff-field select {
  width: 100%;
  border: 1px solid #dedfe2;
  border-radius: 11px;
  outline: none;
  color: #15171b;
  background: #ffffff;
  font-family: inherit;
  font-size: 14px;
  transition: 0.2s ease;
}

.staff-field input,
.staff-field select {
  height: 46px;
  padding: 0 13px;
}

.staff-field textarea {
  min-height: 110px;
  padding: 12px 13px;
  resize: vertical;
  line-height: 1.55;
}

.staff-field input::placeholder,
.staff-field textarea::placeholder {
  color: #a2a5ab;
}

.staff-field input:hover,
.staff-field textarea:hover,
.staff-field select:hover {
  border-color: #c9cace;
}

.staff-field input:focus,
.staff-field textarea:focus,
.staff-field select:focus {
  border-color: #c31320;
  box-shadow: 0 0 0 4px rgba(195, 19, 32, 0.08);
}

.staff-field-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 15px;
}

.staff-modal-actions {
  margin-top: 25px;
  padding-top: 20px;
  display: flex;
  justify-content: flex-end;
  gap: 9px;
  border-top: 1px solid #e9e9ec;
}

.staff-secondary-button {
  min-height: 46px;
  padding: 0 17px;
  border: 1px solid #dedfe2;
  border-radius: 11px;
  color: #5f636b;
  background: #ffffff;
  font-family: inherit;
  font-size: 13px;
  font-weight: 800;
  cursor: pointer;
  transition: 0.2s ease;
}

.staff-secondary-button:hover {
  border-color: #c9cace;
  background: #f7f7f8;
}


/* =========================
   RESPONSIVE
========================= */

@media (max-width: 950px) {
  .staff-event-card {
    grid-template-columns: 40px minmax(0, 1fr) 105px;
  }

  .staff-event-actions {
    grid-column: 2 / -1;
  }

  .staff-event-limit {
    grid-column: 3;
    grid-row: 1;
  }
}


@media (max-width: 800px) {
  .staff-hero {
    align-items: flex-start;
    flex-direction: column;
    padding-top: 55px;
    padding-bottom: 45px;
  }

  .staff-stats {
    grid-template-columns: repeat(2, 1fr);
  }

  .staff-stat:nth-child(3) {
    border-left: 1px solid #e9e9ec;
  }

  .staff-stat {
    border-bottom: 1px solid #e9e9ec;
  }

  .staff-event-card {
    grid-template-columns: 40px 1fr;
    gap: 15px;
  }

  .staff-event-limit {
    grid-column: 2;
    grid-row: auto;
    padding-left: 0;
    border-left: 0;
  }

  .staff-event-actions {
    grid-column: 2;
  }
}


@media (max-width: 720px) {
  .staff-container {
    width: min(100% - 28px, 1180px);
  }

  .staff-header,
  .staff-header-inner {
    min-height: 68px;
  }

  .staff-brand-mark {
    width: 36px;
    height: 36px;
  }

  .staff-brand-name {
    font-size: 13px;
  }

  .staff-nav {
    gap: 3px;
  }

  .staff-nav > a:first-child {
    display: none;
  }

  .staff-nav > a {
    padding: 9px 10px;
    font-size: 12px;
  }

  .staff-role {
    display: none;
  }

  .staff-logout {
    padding: 9px 10px;
    font-size: 12px;
  }

  .staff-hero-content h1 {
    font-size: 46px;
  }

  .staff-hero-content p {
    font-size: 15px;
  }

  .staff-primary-button {
    width: 100%;
  }

  .staff-content {
    padding-top: 65px;
    padding-bottom: 75px;
  }

  .staff-section-heading {
    align-items: flex-start;
    flex-direction: column;
  }

  .staff-section-heading h2 {
    font-size: 29px;
  }

  .staff-field-grid {
    grid-template-columns: 1fr;
    gap: 0;
  }

  .staff-modal-backdrop {
    padding: 15px;
  }

  .staff-modal {
    max-height: calc(100vh - 30px);
    border-radius: 19px;
  }

  .staff-modal-header,
  .staff-form {
    padding: 21px;
  }

  .staff-modal-actions {
    flex-direction: column-reverse;
  }

  .staff-modal-actions button {
    width: 100%;
  }
}


@media (max-width: 460px) {
  .staff-stats {
    grid-template-columns: 1fr;
  }

  .staff-stat {
    border-left: 1px solid #e9e9ec;
  }

  .staff-stat:first-child {
    border-bottom: 1px solid #e9e9ec;
  }

  .staff-event-card {
    padding: 18px;
    grid-template-columns: 30px 1fr;
  }

  .staff-event-number {
    font-size: 10px;
  }

  .staff-event-info h3 {
    font-size: 18px;
  }

  .staff-event-info p {
    white-space: normal;
  }

  .staff-event-actions {
    display: grid;
    grid-template-columns: 1fr 1fr;
  }

  .staff-event-actions button {
    width: 100%;
  }

  .staff-empty {
    padding: 30px 20px;
  }
}
